<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxComments.listFile`

`GET /files/{file_id}/comments`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxComments`, `Comments`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `fileId` | path | `String` | yes |
| `fields` | query | `List<String>` | no |
| `limit_r` | query | `Long` | no |
| `offset` | query | `Long` | no |

## Returns

`Comments`

## Pagination

Paginated by the `offset` cursor. The `Comments` returned here carries `entries` (this page). Advance `offset` by the page size until a page returns no entries:

```apex
Long offset = 0;
Comments page = client.comments.listFile(fileId, null, null, offset);
while (page.entries != null && !page.entries.isEmpty()) {
    // handle page.entries
    offset += page.entries.size();
    page = client.comments.listFile(fileId, null, null, offset);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
Comments result = client.comments.listFile(fileId, null, null, null);
```
