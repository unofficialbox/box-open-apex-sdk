<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxAutomateWorkflows.list_r`

`GET /automate_workflows`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxAutomateWorkflows`, `AutomateWorkflows`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `folderId` | query | `String` | yes |
| `limit_r` | query | `Long` | no |
| `marker` | query | `String` | no |

## Returns

`AutomateWorkflows`

## Pagination

Paginated by the `marker` cursor. The `AutomateWorkflows` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
AutomateWorkflows page = client.automateWorkflows.list_r(folderId, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.automateWorkflows.list_r(folderId, null, page.next_marker);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
AutomateWorkflows result = client.automateWorkflows.list_r(folderId, null, null);
```
