<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxShieldInformationBarrierReports.list_r`

`GET /shield_information_barrier_reports`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxShieldInformationBarrierReports`, `ShieldInformationBarrierReports`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `shieldInformationBarrierId` | query | `String` | yes |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`ShieldInformationBarrierReports`

## Pagination

Paginated by the `marker` cursor. The `ShieldInformationBarrierReports` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
ShieldInformationBarrierReports page = client.shieldInformationBarrierReports.list_r(shieldInformationBarrierId, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.shieldInformationBarrierReports.list_r(shieldInformationBarrierId, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
ShieldInformationBarrierReports result = client.shieldInformationBarrierReports.list_r(shieldInformationBarrierId, null, null);
```
