<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxListCollaborations.listCollaborations`

`GET /collaborations`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxListCollaborations`, `CollaborationsOffsetPaginated`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `status` | query | `String` | yes |
| `fields` | query | `List<String>` | no |
| `offset` | query | `Long` | no |
| `limit_r` | query | `Long` | no |

## Returns

`CollaborationsOffsetPaginated`

## Pagination

Paginated by the `offset` cursor. The `CollaborationsOffsetPaginated` returned here carries `entries` (this page). Advance `offset` by the page size until a page returns no entries:

```apex
Long offset = 0;
CollaborationsOffsetPaginated page = client.listCollaborations.listCollaborations(status, null, offset, null);
while (page.entries != null && !page.entries.isEmpty()) {
    // handle page.entries
    offset += page.entries.size();
    page = client.listCollaborations.listCollaborations(status, null, offset, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
CollaborationsOffsetPaginated result = client.listCollaborations.listCollaborations(status, null, null, null);
```
