<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxListCollaborations.listFolderCollaborations`

`GET /folders/{folder_id}/collaborations`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxListCollaborations`, `Collaborations`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `folderId` | path | `String` | yes |
| `fields` | query | `List<String>` | no |
| `limit_r` | query | `Long` | no |
| `marker` | query | `String` | no |

## Returns

`Collaborations`

## Pagination

Paginated by the `marker` cursor. The `Collaborations` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
Collaborations page = client.listCollaborations.listFolderCollaborations(folderId, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.listCollaborations.listFolderCollaborations(folderId, null, null, page.next_marker);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
Collaborations result = client.listCollaborations.listFolderCollaborations(folderId, null, null, null);
```
