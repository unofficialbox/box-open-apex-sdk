<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxListCollaborations.listGroupCollaborations`

`GET /groups/{group_id}/collaborations`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxListCollaborations`, `CollaborationsOffsetPaginated`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `groupId` | path | `String` | yes |
| `limit_r` | query | `Long` | no |
| `offset` | query | `Long` | no |

## Returns

`CollaborationsOffsetPaginated`

## Pagination

Paginated by the `offset` cursor. The `CollaborationsOffsetPaginated` returned here carries `entries` (this page). Advance `offset` by the page size until a page returns no entries:

```apex
Long offset = 0;
CollaborationsOffsetPaginated page = client.listCollaborations.listGroupCollaborations(groupId, null, offset);
while (page.entries != null && !page.entries.isEmpty()) {
    // handle page.entries
    offset += page.entries.size();
    page = client.listCollaborations.listGroupCollaborations(groupId, null, offset);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
CollaborationsOffsetPaginated result = client.listCollaborations.listGroupCollaborations(groupId, null, null);
```
