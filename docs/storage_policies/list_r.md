<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxStoragePolicies.list_r`

`GET /storage_policies`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxStoragePolicies`, `StoragePolicies`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `fields` | query | `List<String>` | no |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`StoragePolicies`

## Pagination

Paginated by the `marker` cursor. The `StoragePolicies` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
StoragePolicies page = client.storagePolicies.list_r(null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.storagePolicies.list_r(null, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
StoragePolicies result = client.storagePolicies.list_r(null, null, null);
```
