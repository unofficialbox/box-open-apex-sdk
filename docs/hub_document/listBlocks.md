<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxHubDocument.listBlocks`

`GET /hub_document_blocks`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxHubDocument`, `HubDocumentBlocks`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `hubId` | query | `String` | yes |
| `pageId` | query | `String` | yes |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`HubDocumentBlocks`

## Pagination

Paginated by the `marker` cursor. The `HubDocumentBlocks` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
HubDocumentBlocks page = client.hubDocument.listBlocks(hubId, pageId, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.hubDocument.listBlocks(hubId, pageId, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
HubDocumentBlocks result = client.hubDocument.listBlocks(hubId, pageId, null, null);
```
