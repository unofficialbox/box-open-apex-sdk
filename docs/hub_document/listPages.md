<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxHubDocument.listPages`

`GET /hub_document_pages`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxHubDocument`, `HubDocumentPages`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `hubId` | query | `String` | yes |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`HubDocumentPages`

## Pagination

Paginated by the `marker` cursor. The `HubDocumentPages` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
HubDocumentPages page = client.hubDocument.listPages(hubId, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.hubDocument.listPages(hubId, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
HubDocumentPages result = client.hubDocument.listPages(hubId, null, null);
```
