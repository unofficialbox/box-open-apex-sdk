<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxDevicePinners.listEnterprise`

`GET /enterprises/{enterprise_id}/device_pinners`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxDevicePinners`, `DevicePinners`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `enterpriseId` | path | `String` | yes |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |
| `direction` | query | `String` | no |

## Returns

`DevicePinners`

## Pagination

Paginated by the `marker` cursor. The `DevicePinners` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
DevicePinners page = client.devicePinners.listEnterprise(enterpriseId, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.devicePinners.listEnterprise(enterpriseId, page.next_marker, null, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
DevicePinners result = client.devicePinners.listEnterprise(enterpriseId, null, null, null);
```
