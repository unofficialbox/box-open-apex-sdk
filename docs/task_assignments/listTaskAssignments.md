<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxTaskAssignments.listTaskAssignments`

`GET /tasks/{task_id}/assignments`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxTaskAssignments`, `TaskAssignments`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `taskId` | path | `String` | yes |

## Returns

`TaskAssignments`

## Example

```apex
Box client = new Box(myBoxClient);
TaskAssignments result = client.taskAssignments.listTaskAssignments(taskId);
```
