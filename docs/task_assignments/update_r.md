<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxTaskAssignments.update_r`

`PUT /task_assignments/{task_assignment_id}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxTaskAssignments`, `UpdateTaskAssignmentRequest`, `TaskAssignment`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `taskAssignmentId` | path | `String` | yes |

## Request body

`UpdateTaskAssignmentRequest`

## Returns

`TaskAssignment`

## Example

```apex
Box client = new Box(myBoxClient);
TaskAssignment result = client.taskAssignments.update_r(taskAssignmentId, body);
```
