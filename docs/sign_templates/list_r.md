<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxSignTemplates.list_r`

`GET /sign_templates`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxSignTemplates`, `SignTemplates`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`SignTemplates`

## Pagination

Paginated by the `marker` cursor. The `SignTemplates` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
SignTemplates page = client.signTemplates.list_r(null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.signTemplates.list_r(page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
SignTemplates result = client.signTemplates.list_r(null, null);
```
