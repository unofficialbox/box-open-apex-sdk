<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxDocgenTemplate.listJob`

`GET /docgen_template_jobs/{template_id}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxDocgenTemplate`, `DocGenJobs`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `templateId` | path | `String` | yes |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`DocGenJobs`

## Pagination

Paginated by the `marker` cursor. The `DocGenJobs` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
DocGenJobs page = client.docgenTemplate.listJob(templateId, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.docgenTemplate.listJob(templateId, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
DocGenJobs result = client.docgenTemplate.listJob(templateId, null, null);
```
