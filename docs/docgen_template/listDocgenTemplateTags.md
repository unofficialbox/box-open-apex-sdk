<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxDocgenTemplate.listDocgenTemplateTags`

`GET /docgen_templates/{template_id}/tags`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxDocgenTemplate`, `DocGenTags`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `templateId` | path | `String` | yes |
| `templateVersionId` | query | `String` | no |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`DocGenTags`

## Pagination

Paginated by the `marker` cursor. The `DocGenTags` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
DocGenTags page = client.docgenTemplate.listDocgenTemplateTags(templateId, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.docgenTemplate.listDocgenTemplateTags(templateId, null, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
DocGenTags result = client.docgenTemplate.listDocgenTemplateTags(templateId, null, null, null);
```
