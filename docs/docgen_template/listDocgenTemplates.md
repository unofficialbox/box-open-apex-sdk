<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxDocgenTemplate.listDocgenTemplates`

`GET /docgen_templates`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxDocgenTemplate`, `DocGenTemplates`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`DocGenTemplates`

## Pagination

Paginated by the `marker` cursor. The `DocGenTemplates` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
DocGenTemplates page = client.docgenTemplate.listDocgenTemplates(null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.docgenTemplate.listDocgenTemplates(page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
DocGenTemplates result = client.docgenTemplate.listDocgenTemplates(null, null);
```
