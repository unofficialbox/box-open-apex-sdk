<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxArchives.list_r`

`GET /archives`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxArchives`, `Archives`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `limit_r` | query | `Long` | no |
| `marker` | query | `String` | no |

## Returns

`Archives`

## Pagination

Paginated by the `marker` cursor. The `Archives` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
Archives page = client.archives.list_r(null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.archives.list_r(null, page.next_marker);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
Archives result = client.archives.list_r(null, null);
```
