<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxFolderMetadata.createFolderMetadata`

`POST /folders/{folder_id}/metadata/{scope}/{template_key}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxFolderMetadata`, `Map<String, Object>`, `MetadataFull`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `folderId` | path | `String` | yes |
| `scope` | path | `String` | yes |
| `templateKey` | path | `String` | yes |

## Request body

`Map<String, Object>`

## Returns

`MetadataFull`

## Example

```apex
Box client = new Box(myBoxClient);
MetadataFull result = client.folderMetadata.createFolderMetadata(folderId, scope, templateKey, body);
```
