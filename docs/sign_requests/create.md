<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxSignRequests.create`

`POST /sign_requests`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxSignRequests`, `SignRequestCreateRequest`, `SignRequest`

## Parameters

_None._

## Request body

`SignRequestCreateRequest`

## Returns

`SignRequest`

## Example

```apex
Box client = new Box(myBoxClient);
SignRequest result = client.signRequests.create(body);
```
