<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxSignRequests.list_r`

`GET /sign_requests`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxSignRequests`, `SignRequests`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |
| `senders` | query | `List<String>` | no |
| `sharedRequests` | query | `Boolean` | no |

## Returns

`SignRequests`

## Pagination

Paginated by the `marker` cursor. The `SignRequests` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
SignRequests page = client.signRequests.list_r(null, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.signRequests.list_r(page.next_marker, null, null, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
SignRequests result = client.signRequests.list_r(null, null, null, null);
```
