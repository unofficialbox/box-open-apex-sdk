<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxUsers.list_r`

`GET /users`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxUsers`, `Users`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `filterTerm` | query | `String` | no |
| `userType` | query | `String` | no |
| `externalAppUserId` | query | `String` | no |
| `fields` | query | `List<String>` | no |
| `offset` | query | `Long` | no |
| `limit_r` | query | `Long` | no |
| `usemarker` | query | `Boolean` | no |
| `marker` | query | `String` | no |

## Returns

`Users`

## Pagination

Paginated by the `marker` cursor. The `Users` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
Users page = client.users.list_r(null, null, null, null, null, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.users.list_r(null, null, null, null, null, null, null, page.next_marker);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
Users result = client.users.list_r(null, null, null, null, null, null, null, null);
```
