<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxMetadataCascadePolicies.list_r`

`GET /metadata_cascade_policies`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxMetadataCascadePolicies`, `MetadataCascadePolicies`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `folderId` | query | `String` | yes |
| `ownerEnterpriseId` | query | `String` | no |
| `marker` | query | `String` | no |
| `offset` | query | `Long` | no |

## Returns

`MetadataCascadePolicies`

## Pagination

Paginated by the `marker` cursor. The `MetadataCascadePolicies` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
MetadataCascadePolicies page = client.metadataCascadePolicies.list_r(folderId, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.metadataCascadePolicies.list_r(folderId, null, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
MetadataCascadePolicies result = client.metadataCascadePolicies.list_r(folderId, null, null, null);
```
