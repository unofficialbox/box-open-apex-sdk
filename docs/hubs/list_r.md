<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxHubs.list_r`

`GET /hubs`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxHubs`, `Hubs`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `query` | query | `String` | no |
| `scope` | query | `String` | no |
| `sort_r` | query | `String` | no |
| `direction` | query | `String` | no |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`Hubs`

## Pagination

Paginated by the `marker` cursor. The `Hubs` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
Hubs page = client.hubs.list_r(null, null, null, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.hubs.list_r(null, null, null, null, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
Hubs result = client.hubs.list_r(null, null, null, null, null, null);
```
