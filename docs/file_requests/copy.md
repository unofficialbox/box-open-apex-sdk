<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxFileRequests.copy`

`POST /file_requests/{file_request_id}/copy`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxFileRequests`, `FileRequestCopyRequest`, `FileRequest`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `fileRequestId` | path | `String` | yes |

## Request body

`FileRequestCopyRequest`

## Returns

`FileRequest`

## Example

```apex
Box client = new Box(myBoxClient);
FileRequest result = client.fileRequests.copy(fileRequestId, body);
```
