<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxFileRequests.update_r`

`PUT /file_requests/{file_request_id}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxFileRequests`, `FileRequestUpdateRequest`, `FileRequest`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `fileRequestId` | path | `String` | yes |
| `ifMatch` | header | `String` | no |

## Request body

`FileRequestUpdateRequest`

## Returns

`FileRequest`

## Example

```apex
Box client = new Box(myBoxClient);
FileRequest result = client.fileRequests.update_r(fileRequestId, null, body);
```
