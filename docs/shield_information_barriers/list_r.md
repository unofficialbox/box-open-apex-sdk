<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxShieldInformationBarriers.list_r`

`GET /shield_information_barriers`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxShieldInformationBarriers`, `ShieldInformationBarriers`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`ShieldInformationBarriers`

## Pagination

Paginated by the `marker` cursor. The `ShieldInformationBarriers` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
ShieldInformationBarriers page = client.shieldInformationBarriers.list_r(null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.shieldInformationBarriers.list_r(page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
ShieldInformationBarriers result = client.shieldInformationBarriers.list_r(null, null);
```
