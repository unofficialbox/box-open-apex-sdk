<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxAppItemAssociations.listFolder`

`GET /folders/{folder_id}/app_item_associations`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxAppItemAssociations`, `AppItemAssociations`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `folderId` | path | `String` | yes |
| `limit_r` | query | `Long` | no |
| `marker` | query | `String` | no |
| `applicationType` | query | `String` | no |

## Returns

`AppItemAssociations`

## Pagination

Paginated by the `marker` cursor. The `AppItemAssociations` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
AppItemAssociations page = client.appItemAssociations.listFolder(folderId, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.appItemAssociations.listFolder(folderId, null, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
AppItemAssociations result = client.appItemAssociations.listFolder(folderId, null, null, null);
```
