<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxCollaborationAllowlistExemptTargets.createCollaborationWhitelistExemptTargets`

`POST /collaboration_whitelist_exempt_targets`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxCollaborationAllowlistExemptTargets`, `CollaborationWhitelistExemptTarg_1123717`, `CollaborationAllowlistExemptTarget`

## Parameters

_None._

## Request body

`CollaborationWhitelistExemptTarg_1123717`

## Returns

`CollaborationAllowlistExemptTarget`

## Example

```apex
Box client = new Box(myBoxClient);
CollaborationAllowlistExemptTarget result = client.collaborationAllowlistExemptTargets.createCollaborationWhitelistExemptTargets(body);
```
