<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxCollaborationAllowlistExemptTargets.getCollaborationWhitelistExemptTarget`

`GET /collaboration_whitelist_exempt_targets/{collaboration_whitelist_exempt_target_id}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxCollaborationAllowlistExemptTargets`, `CollaborationAllowlistExemptTarget`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `collaborationWhitelistExemptTargetId` | path | `String` | yes |

## Returns

`CollaborationAllowlistExemptTarget`

## Example

```apex
Box client = new Box(myBoxClient);
CollaborationAllowlistExemptTarget result = client.collaborationAllowlistExemptTargets.getCollaborationWhitelistExemptTarget(collaborationWhitelistExemptTargetId);
```
