<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxCollaborationAllowlistExemptTargets.create`

`POST /collaboration_whitelist_exempt_targets`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxCollaborationAllowlistExemptTargets`, `CreateCollaborationAllowlistExem_064059e`, `CollaborationAllowlistExemptTarget`

## Parameters

_None._

## Request body

`CreateCollaborationAllowlistExem_064059e`

## Returns

`CollaborationAllowlistExemptTarget`

## Example

```apex
Box client = new Box(myBoxClient);
CollaborationAllowlistExemptTarget result = client.collaborationAllowlistExemptTargets.create(body);
```
