<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxRecentItems.list_r`

`GET /recent_items`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxRecentItems`, `RecentItems`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `fields` | query | `List<String>` | no |
| `limit_r` | query | `Long` | no |
| `marker` | query | `String` | no |

## Returns

`RecentItems`

## Pagination

Paginated by the `marker` cursor. The `RecentItems` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
RecentItems page = client.recentItems.list_r(null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.recentItems.list_r(null, null, page.next_marker);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
RecentItems result = client.recentItems.list_r(null, null, null);
```
