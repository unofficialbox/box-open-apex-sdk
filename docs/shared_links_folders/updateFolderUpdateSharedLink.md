<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxSharedLinksFolders.updateFolderUpdateSharedLink`

`PUT /folders/{folder_id}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxSharedLinksFolders`, `UpdateFolderUpdateSharedLinkRequest`, `FolderFull`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `folderId` | path | `String` | yes |
| `fields` | query | `String` | yes |

## Request body

`UpdateFolderUpdateSharedLinkRequest`

## Returns

`FolderFull`

## Example

```apex
Box client = new Box(myBoxClient);
FolderFull result = client.sharedLinksFolders.updateFolderUpdateSharedLink(folderId, fields, body);
```
