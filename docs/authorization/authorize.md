<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxAuthorization.authorize`

`GET /authorize`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxAuthorization`, `String`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `responseType` | query | `String` | yes |
| `clientId` | query | `String` | yes |
| `redirectUri` | query | `String` | no |
| `state` | query | `String` | no |
| `scope` | query | `String` | no |

## Returns

`String`

## Example

```apex
Box client = new Box(myBoxClient);
String result = client.authorization.authorize(responseType, clientId, null, null, null);
```
