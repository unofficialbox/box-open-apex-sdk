<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxAuthorization.createOauth2TokenRefresh`

`POST /oauth2/token`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxAuthorization`, `PostOAuth2TokenRefreshAccessToken`, `AccessToken`

## Parameters

_None._

## Request body

`PostOAuth2TokenRefreshAccessToken`

## Returns

`AccessToken`

## Example

```apex
Box client = new Box(myBoxClient);
AccessToken result = client.authorization.createOauth2TokenRefresh(body);
```
