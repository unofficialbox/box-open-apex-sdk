<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxTransfer.updateUserFolder0`

`PUT /users/{user_id}/folders/0`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxTransfer`, `UserFolder0UpdateRequest`, `FolderFull`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `userId` | path | `String` | yes |
| `fields` | query | `List<String>` | no |
| `notify` | query | `Boolean` | no |

## Request body

`UserFolder0UpdateRequest`

## Returns

`FolderFull`

## Example

```apex
Box client = new Box(myBoxClient);
FolderFull result = client.transfer.updateUserFolder0(userId, null, null, body);
```
