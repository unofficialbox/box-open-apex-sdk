<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxFileVersions.updateFileVersion`

`PUT /files/{file_id}/versions/{file_version_id}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxFileVersions`, `FileVersionUpdateRequest`, `FileVersionFull`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `fileId` | path | `String` | yes |
| `fileVersionId` | path | `String` | yes |

## Request body

`FileVersionUpdateRequest`

## Returns

`FileVersionFull`

## Example

```apex
Box client = new Box(myBoxClient);
FileVersionFull result = client.fileVersions.updateFileVersion(fileId, fileVersionId, body);
```
