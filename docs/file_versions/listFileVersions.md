<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxFileVersions.listFileVersions`

`GET /files/{file_id}/versions`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxFileVersions`, `FileVersions`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `fileId` | path | `String` | yes |
| `fields` | query | `List<String>` | no |
| `limit_r` | query | `Long` | no |
| `offset` | query | `Long` | no |

## Returns

`FileVersions`

## Pagination

Paginated by the `offset` cursor. The `FileVersions` returned here carries `entries` (this page). Advance `offset` by the page size until a page returns no entries:

```apex
Long offset = 0;
FileVersions page = client.fileVersions.listFileVersions(fileId, null, null, offset);
while (page.entries != null && !page.entries.isEmpty()) {
    // handle page.entries
    offset += page.entries.size();
    page = client.fileVersions.listFileVersions(fileId, null, null, offset);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
FileVersions result = client.fileVersions.listFileVersions(fileId, null, null, null);
```
