<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxFileVersions.createFileVersionCurrent`

`POST /files/{file_id}/versions/current`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxFileVersions`, `FileVersionCurrentCreateRequest`, `FileVersionFull`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `fileId` | path | `String` | yes |
| `fields` | query | `List<String>` | no |

## Request body

`FileVersionCurrentCreateRequest`

## Returns

`FileVersionFull`

## Example

```apex
Box client = new Box(myBoxClient);
FileVersionFull result = client.fileVersions.createFileVersionCurrent(fileId, null, body);
```
