<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxZipDownloads.getStatus`

`GET /zip_downloads/{zip_download_id}/status`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxZipDownloads`, `ZipDownloadStatus`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `zipDownloadId` | path | `String` | yes |

## Returns

`ZipDownloadStatus`

## Example

```apex
Box client = new Box(myBoxClient);
ZipDownloadStatus result = client.zipDownloads.getStatus(zipDownloadId);
```
