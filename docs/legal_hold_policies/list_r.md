<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxLegalHoldPolicies.list_r`

`GET /legal_hold_policies`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxLegalHoldPolicies`, `LegalHoldPolicies`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `policyName` | query | `String` | no |
| `fields` | query | `List<String>` | no |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`LegalHoldPolicies`

## Pagination

Paginated by the `marker` cursor. The `LegalHoldPolicies` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
LegalHoldPolicies page = client.legalHoldPolicies.list_r(null, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.legalHoldPolicies.list_r(null, null, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
LegalHoldPolicies result = client.legalHoldPolicies.list_r(null, null, null, null);
```
