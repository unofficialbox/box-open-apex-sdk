<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxSkills.update_r`

`PUT /files/{file_id}/metadata/global/boxSkillsCards`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxSkills`, `List<SkillUpdateRequest>`, `SkillCardsMetadata`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `fileId` | path | `String` | yes |

## Request body

`List<SkillUpdateRequest>`

## Returns

`SkillCardsMetadata`

## Example

```apex
Box client = new Box(myBoxClient);
SkillCardsMetadata result = client.skills.update_r(fileId, body);
```
