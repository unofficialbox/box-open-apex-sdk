<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxFileVersionLegalHolds.get`

`GET /file_version_legal_holds/{file_version_legal_hold_id}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxFileVersionLegalHolds`, `FileVersionLegalHold`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `fileVersionLegalHoldId` | path | `String` | yes |

## Returns

`FileVersionLegalHold`

## Example

```apex
Box client = new Box(myBoxClient);
FileVersionLegalHold result = client.fileVersionLegalHolds.get(fileVersionLegalHoldId);
```
