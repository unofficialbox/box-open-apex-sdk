<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxFileVersionLegalHolds.list_r`

`GET /file_version_legal_holds`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxFileVersionLegalHolds`, `FileVersionLegalHolds`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `policyId` | query | `String` | yes |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`FileVersionLegalHolds`

## Pagination

Paginated by the `marker` cursor. The `FileVersionLegalHolds` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
FileVersionLegalHolds page = client.fileVersionLegalHolds.list_r(policyId, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.fileVersionLegalHolds.list_r(policyId, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
FileVersionLegalHolds result = client.fileVersionLegalHolds.list_r(policyId, null, null);
```
