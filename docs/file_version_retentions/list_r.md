<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxFileVersionRetentions.list_r`

`GET /file_version_retentions`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxFileVersionRetentions`, `FileVersionRetentions`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `fileId` | query | `String` | no |
| `fileVersionId` | query | `String` | no |
| `policyId` | query | `String` | no |
| `dispositionAction` | query | `String` | no |
| `dispositionBefore` | query | `String` | no |
| `dispositionAfter` | query | `String` | no |
| `limit_r` | query | `Long` | no |
| `marker` | query | `String` | no |

## Returns

`FileVersionRetentions`

## Pagination

Paginated by the `marker` cursor. The `FileVersionRetentions` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
FileVersionRetentions page = client.fileVersionRetentions.list_r(null, null, null, null, null, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.fileVersionRetentions.list_r(null, null, null, null, null, null, null, page.next_marker);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
FileVersionRetentions result = client.fileVersionRetentions.list_r(null, null, null, null, null, null, null, null);
```
