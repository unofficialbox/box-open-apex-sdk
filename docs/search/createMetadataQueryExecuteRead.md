<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxSearch.createMetadataQueryExecuteRead`

`POST /metadata_queries/execute_read`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxSearch`, `MetadataQuery`, `MetadataQueryResults`

## Parameters

_None._

## Request body

`MetadataQuery`

## Returns

`MetadataQueryResults`

## Example

```apex
Box client = new Box(myBoxClient);
MetadataQueryResults result = client.search.createMetadataQueryExecuteRead(body);
```
