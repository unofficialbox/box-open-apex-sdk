<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxMetadataTaxonomies.list_r`

`GET /metadata_taxonomies/{namespace}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxMetadataTaxonomies`, `MetadataTaxonomies`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `namespace` | path | `String` | yes |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`MetadataTaxonomies`

## Pagination

Paginated by the `marker` cursor. The `MetadataTaxonomies` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
MetadataTaxonomies page = client.metadataTaxonomies.list_r(namespace, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.metadataTaxonomies.list_r(namespace, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
MetadataTaxonomies result = client.metadataTaxonomies.list_r(namespace, null, null);
```
