<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxMetadataTaxonomies.listNodes`

`GET /metadata_taxonomies/{namespace}/{taxonomy_key}/nodes`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxMetadataTaxonomies`, `MetadataTaxonomyNodes`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `namespace` | path | `String` | yes |
| `taxonomyKey` | path | `String` | yes |
| `level` | query | `List<Long>` | no |
| `parent` | query | `List<String>` | no |
| `ancestor` | query | `List<String>` | no |
| `query` | query | `String` | no |
| `includeTotalResultCount` | query | `Boolean` | no |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`MetadataTaxonomyNodes`

## Pagination

Paginated by the `marker` cursor. The `MetadataTaxonomyNodes` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
MetadataTaxonomyNodes page = client.metadataTaxonomies.listNodes(namespace, taxonomyKey, null, null, null, null, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.metadataTaxonomies.listNodes(namespace, taxonomyKey, null, null, null, null, null, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
MetadataTaxonomyNodes result = client.metadataTaxonomies.listNodes(namespace, taxonomyKey, null, null, null, null, null, null, null);
```
