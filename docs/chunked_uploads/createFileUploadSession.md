<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxChunkedUploads.createFileUploadSession`

`POST /files/upload_sessions`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxChunkedUploads`, `FileUploadSessionCreateRequest`, `UploadSession`

## Parameters

_None._

## Request body

`FileUploadSessionCreateRequest`

## Returns

`UploadSession`

## Example

```apex
Box client = new Box(myBoxClient);
UploadSession result = client.chunkedUploads.createFileUploadSession(body);
```
