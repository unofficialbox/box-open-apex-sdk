<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxChunkedUploads.updateFileUploadSession`

`PUT /files/upload_sessions/{upload_session_id}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxChunkedUploads`, `Blob`, `UploadedPart`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `uploadSessionId` | path | `String` | yes |
| `digest` | header | `String` | yes |
| `contentRange` | header | `String` | yes |

## Request body

`Blob`

## Returns

`UploadedPart`

## Example

```apex
Box client = new Box(myBoxClient);
UploadedPart result = client.chunkedUploads.updateFileUploadSession(uploadSessionId, digest, contentRange, body);
```
