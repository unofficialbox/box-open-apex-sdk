<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxChunkedUploads.listFileUploadSessionParts`

`GET /files/upload_sessions/{upload_session_id}/parts`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxChunkedUploads`, `UploadParts`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `uploadSessionId` | path | `String` | yes |
| `offset` | query | `Long` | no |
| `limit_r` | query | `Long` | no |

## Returns

`UploadParts`

## Pagination

Paginated by the `offset` cursor. The `UploadParts` returned here carries `entries` (this page). Advance `offset` by the page size until a page returns no entries:

```apex
Long offset = 0;
UploadParts page = client.chunkedUploads.listFileUploadSessionParts(uploadSessionId, offset, null);
while (page.entries != null && !page.entries.isEmpty()) {
    // handle page.entries
    offset += page.entries.size();
    page = client.chunkedUploads.listFileUploadSessionParts(uploadSessionId, offset, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
UploadParts result = client.chunkedUploads.listFileUploadSessionParts(uploadSessionId, null, null);
```
