<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxHubCollaborations.list_r`

`GET /hub_collaborations`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxHubCollaborations`, `HubCollaborations`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `hubId` | query | `String` | yes |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`HubCollaborations`

## Pagination

Paginated by the `marker` cursor. The `HubCollaborations` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
HubCollaborations page = client.hubCollaborations.list_r(hubId, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.hubCollaborations.list_r(hubId, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
HubCollaborations result = client.hubCollaborations.list_r(hubId, null, null);
```
