<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxHubCollaborations.get`

`GET /hub_collaborations/{hub_collaboration_id}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxHubCollaborations`, `HubCollaboration`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `hubCollaborationId` | path | `String` | yes |

## Returns

`HubCollaboration`

## Example

```apex
Box client = new Box(myBoxClient);
HubCollaboration result = client.hubCollaborations.get(hubCollaborationId);
```
