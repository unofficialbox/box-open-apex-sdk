<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxRetentionPolicyAssignments.listRetentionPolicyAssignments`

`GET /retention_policies/{retention_policy_id}/assignments`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxRetentionPolicyAssignments`, `RetentionPolicyAssignments`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `retentionPolicyId` | path | `String` | yes |
| `type` | query | `String` | no |
| `fields` | query | `List<String>` | no |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`RetentionPolicyAssignments`

## Pagination

Paginated by the `marker` cursor. The `RetentionPolicyAssignments` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
RetentionPolicyAssignments page = client.retentionPolicyAssignments.listRetentionPolicyAssignments(retentionPolicyId, null, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.retentionPolicyAssignments.listRetentionPolicyAssignments(retentionPolicyId, null, null, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
RetentionPolicyAssignments result = client.retentionPolicyAssignments.listRetentionPolicyAssignments(retentionPolicyId, null, null, null, null);
```
