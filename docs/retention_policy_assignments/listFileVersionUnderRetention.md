<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxRetentionPolicyAssignments.listFileVersionUnderRetention`

`GET /retention_policy_assignments/{retention_policy_assignment_id}/file_versions_under_retention`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxRetentionPolicyAssignments`, `FilesUnderRetention`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `retentionPolicyAssignmentId` | path | `String` | yes |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`FilesUnderRetention`

## Pagination

Paginated by the `marker` cursor. The `FilesUnderRetention` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
FilesUnderRetention page = client.retentionPolicyAssignments.listFileVersionUnderRetention(retentionPolicyAssignmentId, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.retentionPolicyAssignments.listFileVersionUnderRetention(retentionPolicyAssignmentId, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
FilesUnderRetention result = client.retentionPolicyAssignments.listFileVersionUnderRetention(retentionPolicyAssignmentId, null, null);
```
