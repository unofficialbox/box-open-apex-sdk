<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxEnterpriseConfigurations.get`

`GET /enterprise_configurations/{enterprise_id}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxEnterpriseConfigurations`, `EnterpriseConfiguration`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `enterpriseId` | path | `String` | yes |
| `categories` | query | `List<String>` | yes |

## Returns

`EnterpriseConfiguration`

## Example

```apex
Box client = new Box(myBoxClient);
EnterpriseConfiguration result = client.enterpriseConfigurations.get(enterpriseId, categories);
```
