<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxWorkflows.list_r`

`GET /workflows`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxWorkflows`, `Workflows`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `folderId` | query | `String` | yes |
| `triggerType` | query | `String` | no |
| `limit_r` | query | `Long` | no |
| `marker` | query | `String` | no |

## Returns

`Workflows`

## Pagination

Paginated by the `marker` cursor. The `Workflows` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
Workflows page = client.workflows.list_r(folderId, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.workflows.list_r(folderId, null, null, page.next_marker);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
Workflows result = client.workflows.list_r(folderId, null, null, null);
```
