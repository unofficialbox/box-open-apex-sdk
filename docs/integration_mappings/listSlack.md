<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxIntegrationMappings.listSlack`

`GET /integration_mappings/slack`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxIntegrationMappings`, `IntegrationMappings`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |
| `partnerItemType` | query | `String` | no |
| `partnerItemId` | query | `String` | no |
| `boxItemId` | query | `String` | no |
| `boxItemType` | query | `String` | no |
| `isManuallyCreated` | query | `Boolean` | no |

## Returns

`IntegrationMappings`

## Pagination

Paginated by the `marker` cursor. The `IntegrationMappings` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
IntegrationMappings page = client.integrationMappings.listSlack(null, null, null, null, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.integrationMappings.listSlack(page.next_marker, null, null, null, null, null, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
IntegrationMappings result = client.integrationMappings.listSlack(null, null, null, null, null, null, null);
```
