<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxIntegrationMappings.updateSlack`

`PUT /integration_mappings/slack/{integration_mapping_id}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxIntegrationMappings`, `SlackUpdateRequest`, `IntegrationMapping`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `integrationMappingId` | path | `String` | yes |

## Request body

`SlackUpdateRequest`

## Returns

`IntegrationMapping`

## Example

```apex
Box client = new Box(myBoxClient);
IntegrationMapping result = client.integrationMappings.updateSlack(integrationMappingId, body);
```
