<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxIntegrationMappings.updateTeam`

`PUT /integration_mappings/teams/{integration_mapping_id}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxIntegrationMappings`, `TeamUpdateRequest`, `IntegrationMappingTeams`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `integrationMappingId` | path | `String` | yes |

## Request body

`TeamUpdateRequest`

## Returns

`IntegrationMappingTeams`

## Example

```apex
Box client = new Box(myBoxClient);
IntegrationMappingTeams result = client.integrationMappings.updateTeam(integrationMappingId, body);
```
