<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxIntegrationMappings.createSlack`

`POST /integration_mappings/slack`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxIntegrationMappings`, `IntegrationMappingSlackCreateRequest`, `IntegrationMapping`

## Parameters

_None._

## Request body

`IntegrationMappingSlackCreateRequest`

## Returns

`IntegrationMapping`

## Example

```apex
Box client = new Box(myBoxClient);
IntegrationMapping result = client.integrationMappings.createSlack(body);
```
