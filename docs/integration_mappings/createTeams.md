<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxIntegrationMappings.createTeams`

`POST /integration_mappings/teams`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxIntegrationMappings`, `IntegrationMappingTeamsCreateRequest`, `IntegrationMappingTeams`

## Parameters

_None._

## Request body

`IntegrationMappingTeamsCreateRequest`

## Returns

`IntegrationMappingTeams`

## Example

```apex
Box client = new Box(myBoxClient);
IntegrationMappingTeams result = client.integrationMappings.createTeams(body);
```
