<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxMemberships.listGroup`

`GET /groups/{group_id}/memberships`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxMemberships`, `GroupMemberships`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `groupId` | path | `String` | yes |
| `limit_r` | query | `Long` | no |
| `offset` | query | `Long` | no |

## Returns

`GroupMemberships`

## Pagination

Paginated by the `offset` cursor. The `GroupMemberships` returned here carries `entries` (this page). Advance `offset` by the page size until a page returns no entries:

```apex
Long offset = 0;
GroupMemberships page = client.memberships.listGroup(groupId, null, offset);
while (page.entries != null && !page.entries.isEmpty()) {
    // handle page.entries
    offset += page.entries.size();
    page = client.memberships.listGroup(groupId, null, offset);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
GroupMemberships result = client.memberships.listGroup(groupId, null, null);
```
