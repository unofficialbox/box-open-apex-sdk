<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxFolders.listItems`

`GET /folders/{folder_id}/items`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxFolders`, `Items`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `folderId` | path | `String` | yes |
| `fields` | query | `List<String>` | no |
| `usemarker` | query | `Boolean` | no |
| `marker` | query | `String` | no |
| `offset` | query | `Long` | no |
| `limit_r` | query | `Long` | no |
| `boxapi` | header | `String` | no |
| `sort_r` | query | `String` | no |
| `direction` | query | `String` | no |

## Returns

`Items`

## Pagination

Paginated by the `marker` cursor. The `Items` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
Items page = client.folders.listItems(folderId, null, null, null, null, null, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.folders.listItems(folderId, null, null, page.next_marker, null, null, null, null, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
Items result = client.folders.listItems(folderId, null, null, null, null, null, null, null, null);
```
