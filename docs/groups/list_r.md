<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxGroups.list_r`

`GET /groups`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxGroups`, `Groups`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `filterTerm` | query | `String` | no |
| `fields` | query | `List<String>` | no |
| `limit_r` | query | `Long` | no |
| `offset` | query | `Long` | no |

## Returns

`Groups`

## Pagination

Paginated by the `offset` cursor. The `Groups` returned here carries `entries` (this page). Advance `offset` by the page size until a page returns no entries:

```apex
Long offset = 0;
Groups page = client.groups.list_r(null, null, null, offset);
while (page.entries != null && !page.entries.isEmpty()) {
    // handle page.entries
    offset += page.entries.size();
    page = client.groups.list_r(null, null, null, offset);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
Groups result = client.groups.list_r(null, null, null, null);
```
