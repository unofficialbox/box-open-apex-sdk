<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxHubItems.list_r`

`GET /hub_items`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxHubItems`, `HubItems`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `hubId` | query | `String` | yes |
| `parentId` | query | `String` | no |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`HubItems`

## Pagination

Paginated by the `marker` cursor. The `HubItems` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
HubItems page = client.hubItems.list_r(hubId, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.hubItems.list_r(hubId, null, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
HubItems result = client.hubItems.list_r(hubId, null, null, null);
```
