<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxAiStudio.listAiAgents`

`GET /ai_agents`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxAiStudio`, `AiMultipleAgentResponse`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `mode` | query | `List<String>` | no |
| `fields` | query | `List<String>` | no |
| `agentState` | query | `List<String>` | no |
| `includeBoxDefault` | query | `Boolean` | no |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`AiMultipleAgentResponse`

## Pagination

Paginated by the `marker` cursor. The `AiMultipleAgentResponse` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
AiMultipleAgentResponse page = client.aiStudio.listAiAgents(null, null, null, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.aiStudio.listAiAgents(null, null, null, null, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
AiMultipleAgentResponse result = client.aiStudio.listAiAgents(null, null, null, null, null, null);
```
