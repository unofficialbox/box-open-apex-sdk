<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxLegalHoldPolicyAssignments.list_r`

`GET /legal_hold_policy_assignments`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxLegalHoldPolicyAssignments`, `LegalHoldPolicyAssignments`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `policyId` | query | `String` | yes |
| `assignToType` | query | `String` | no |
| `assignToId` | query | `String` | no |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |
| `fields` | query | `List<String>` | no |

## Returns

`LegalHoldPolicyAssignments`

## Pagination

Paginated by the `marker` cursor. The `LegalHoldPolicyAssignments` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
LegalHoldPolicyAssignments page = client.legalHoldPolicyAssignments.list_r(policyId, null, null, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.legalHoldPolicyAssignments.list_r(policyId, null, null, page.next_marker, null, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
LegalHoldPolicyAssignments result = client.legalHoldPolicyAssignments.list_r(policyId, null, null, null, null, null);
```
