<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxLegalHoldPolicyAssignments.listFileOnHold`

`GET /legal_hold_policy_assignments/{legal_hold_policy_assignment_id}/files_on_hold`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxLegalHoldPolicyAssignments`, `FilesOnHold`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `legalHoldPolicyAssignmentId` | path | `String` | yes |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |
| `fields` | query | `List<String>` | no |

## Returns

`FilesOnHold`

## Pagination

Paginated by the `marker` cursor. The `FilesOnHold` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
FilesOnHold page = client.legalHoldPolicyAssignments.listFileOnHold(legalHoldPolicyAssignmentId, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.legalHoldPolicyAssignments.listFileOnHold(legalHoldPolicyAssignmentId, page.next_marker, null, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
FilesOnHold result = client.legalHoldPolicyAssignments.listFileOnHold(legalHoldPolicyAssignmentId, null, null, null);
```
