<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxLegalHoldPolicyAssignments.listFileVersionOnHold`

`GET /legal_hold_policy_assignments/{legal_hold_policy_assignment_id}/file_versions_on_hold`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxLegalHoldPolicyAssignments`, `FileVersionsOnHold`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `legalHoldPolicyAssignmentId` | path | `String` | yes |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |
| `fields` | query | `List<String>` | no |

## Returns

`FileVersionsOnHold`

## Pagination

Paginated by the `marker` cursor. The `FileVersionsOnHold` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
FileVersionsOnHold page = client.legalHoldPolicyAssignments.listFileVersionOnHold(legalHoldPolicyAssignmentId, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.legalHoldPolicyAssignments.listFileVersionOnHold(legalHoldPolicyAssignmentId, page.next_marker, null, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
FileVersionsOnHold result = client.legalHoldPolicyAssignments.listFileVersionOnHold(legalHoldPolicyAssignmentId, null, null, null);
```
