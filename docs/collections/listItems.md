<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxCollections.listItems`

`GET /collections/{collection_id}/items`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxCollections`, `ItemsOffsetPaginated`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `collectionId` | path | `String` | yes |
| `fields` | query | `List<String>` | no |
| `offset` | query | `Long` | no |
| `limit_r` | query | `Long` | no |

## Returns

`ItemsOffsetPaginated`

## Pagination

Paginated by the `offset` cursor. The `ItemsOffsetPaginated` returned here carries `entries` (this page). Advance `offset` by the page size until a page returns no entries:

```apex
Long offset = 0;
ItemsOffsetPaginated page = client.collections.listItems(collectionId, null, offset, null);
while (page.entries != null && !page.entries.isEmpty()) {
    // handle page.entries
    offset += page.entries.size();
    page = client.collections.listItems(collectionId, null, offset, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
ItemsOffsetPaginated result = client.collections.listItems(collectionId, null, null, null);
```
