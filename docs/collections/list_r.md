<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxCollections.list_r`

`GET /collections`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxCollections`, `Collections`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `fields` | query | `List<String>` | no |
| `offset` | query | `Long` | no |
| `limit_r` | query | `Long` | no |

## Returns

`Collections`

## Pagination

Paginated by the `offset` cursor. The `Collections` returned here carries `entries` (this page). Advance `offset` by the page size until a page returns no entries:

```apex
Long offset = 0;
Collections page = client.collections.list_r(null, offset, null);
while (page.entries != null && !page.entries.isEmpty()) {
    // handle page.entries
    offset += page.entries.size();
    page = client.collections.list_r(null, offset, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
Collections result = client.collections.list_r(null, null, null);
```
