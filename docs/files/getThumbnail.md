<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxFiles.getThumbnail`

`GET /files/{file_id}/thumbnail.{extension}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxFiles`, `Blob`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `fileId` | path | `String` | yes |
| `extension` | path | `String` | yes |
| `minHeight` | query | `Long` | no |
| `minWidth` | query | `Long` | no |
| `maxHeight` | query | `Long` | no |
| `maxWidth` | query | `Long` | no |

## Returns

`Blob`

## Example

```apex
Box client = new Box(myBoxClient);
Blob result = client.files.getThumbnail(fileId, extension, null, null, null, null);
```
