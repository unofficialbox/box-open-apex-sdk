<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxShieldInformationBarrierSegments.list_r`

`GET /shield_information_barrier_segments`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxShieldInformationBarrierSegments`, `ShieldInformationBarrierSegments`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `shieldInformationBarrierId` | query | `String` | yes |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`ShieldInformationBarrierSegments`

## Pagination

Paginated by the `marker` cursor. The `ShieldInformationBarrierSegments` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
ShieldInformationBarrierSegments page = client.shieldInformationBarrierSegments.list_r(shieldInformationBarrierId, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.shieldInformationBarrierSegments.list_r(shieldInformationBarrierId, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
ShieldInformationBarrierSegments result = client.shieldInformationBarrierSegments.list_r(shieldInformationBarrierId, null, null);
```
