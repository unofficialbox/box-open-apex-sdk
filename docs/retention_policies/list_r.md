<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxRetentionPolicies.list_r`

`GET /retention_policies`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxRetentionPolicies`, `RetentionPolicies`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `policyName` | query | `String` | no |
| `policyType` | query | `String` | no |
| `createdByUserId` | query | `String` | no |
| `fields` | query | `List<String>` | no |
| `limit_r` | query | `Long` | no |
| `marker` | query | `String` | no |

## Returns

`RetentionPolicies`

## Pagination

Paginated by the `marker` cursor. The `RetentionPolicies` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
RetentionPolicies page = client.retentionPolicies.list_r(null, null, null, null, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.retentionPolicies.list_r(null, null, null, null, null, page.next_marker);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
RetentionPolicies result = client.retentionPolicies.list_r(null, null, null, null, null, null);
```
