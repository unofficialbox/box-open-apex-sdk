<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxDocgen.listJobs`

`GET /docgen_jobs`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxDocgen`, `DocGenJobsFull`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`DocGenJobsFull`

## Pagination

Paginated by the `marker` cursor. The `DocGenJobsFull` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
DocGenJobsFull page = client.docgen.listJobs(null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.docgen.listJobs(page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
DocGenJobsFull result = client.docgen.listJobs(null, null);
```
