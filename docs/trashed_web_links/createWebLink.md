<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxTrashedWebLinks.createWebLink`

`POST /web_links/{web_link_id}`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxTrashedWebLinks`, `CreateFileRequest`, `TrashWebLinkRestored`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `webLinkId` | path | `String` | yes |
| `fields` | query | `List<String>` | no |

## Request body

`CreateFileRequest`

## Returns

`TrashWebLinkRestored`

## Example

```apex
Box client = new Box(myBoxClient);
TrashWebLinkRestored result = client.trashedWebLinks.createWebLink(webLinkId, null, body);
```
