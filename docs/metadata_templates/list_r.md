<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxMetadataTemplates.list_r`

`GET /metadata_templates`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxMetadataTemplates`, `MetadataTemplates`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `metadataInstanceId` | query | `String` | yes |
| `marker` | query | `String` | no |
| `limit_r` | query | `Long` | no |

## Returns

`MetadataTemplates`

## Pagination

Paginated by the `marker` cursor. The `MetadataTemplates` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
MetadataTemplates page = client.metadataTemplates.list_r(metadataInstanceId, null, null);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.metadataTemplates.list_r(metadataInstanceId, page.next_marker, null);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
MetadataTemplates result = client.metadataTemplates.list_r(metadataInstanceId, null, null);
```
