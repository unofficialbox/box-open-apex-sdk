<!-- Generated by box-gantry. DO NOT EDIT. -->
# `BoxStoragePolicyAssignments.list_r`

`GET /storage_policy_assignments`

## Imports & setup

Apex has no `import` statement — every generated SDK class is visible
in the org namespace. Construct the entry-point client once:

```apex
Box client = new Box(myBoxClient); // myBoxClient implements BoxClient
```

**SDK types used:** `Box`, `BoxStoragePolicyAssignments`, `StoragePolicyAssignments`

## Parameters

| Name | In | Type | Required |
|---|---|---|---|
| `marker` | query | `String` | no |
| `resolvedForType` | query | `String` | yes |
| `resolvedForId` | query | `String` | yes |

## Returns

`StoragePolicyAssignments`

## Pagination

Paginated by the `marker` cursor. The `StoragePolicyAssignments` returned here carries `entries` (this page) and `next_marker` (the cursor for the next page — blank on the last). Loop until the cursor is blank:

```apex
StoragePolicyAssignments page = client.storagePolicyAssignments.list_r(null, resolvedForType, resolvedForId);
while (String.isNotBlank(page.next_marker)) {
    // handle page.entries
    page = client.storagePolicyAssignments.list_r(page.next_marker, resolvedForType, resolvedForId);
}
```

## Example

```apex
Box client = new Box(myBoxClient);
StoragePolicyAssignments result = client.storagePolicyAssignments.list_r(null, resolvedForType, resolvedForId);
```
