// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `folder_metadata` resource manager — 5 operation(s).
 * Reached through the `Box` client (`client.folderMetadata`), never
 * constructed directly.
 */
public with sharing class BoxFolderMetadata {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxFolderMetadata(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /folders/{folder_id}/metadata`
     *
     * @param folderId path parameter
     * @param view query parameter, optional
     * @return `Metadatas`
     */
    public Metadatas listFolderMetadata(String folderId, String view) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'folders' + '/' + folderId + '/' + 'metadata';
        if (view != null) request.query.put('view', String.valueOf(view));
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = Metadatas.normalizeKeys((Map<String, Object>) parsed);
        return (Metadatas) JSON.deserialize(JSON.serialize(parsed), Metadatas.class);
    }

    /**
     * `GET /folders/{folder_id}/metadata/{scope}/{template_key}`
     *
     * @param folderId path parameter
     * @param scope path parameter
     * @param templateKey path parameter
     * @return `MetadataFull`
     */
    public MetadataFull getFolderMetadata(String folderId, String scope, String templateKey) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'folders' + '/' + folderId + '/' + 'metadata' + '/' + scope + '/' + templateKey;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = MetadataFull.normalizeKeys((Map<String, Object>) parsed);
        return (MetadataFull) JSON.deserialize(JSON.serialize(parsed), MetadataFull.class);
    }

    /**
     * `PUT /folders/{folder_id}/metadata/{scope}/{template_key}`
     *
     * @param folderId path parameter
     * @param scope path parameter
     * @param templateKey path parameter
     * @param body the request body (`List<FileMetadataUpdateRequest>`)
     * @return `MetadataFull`
     */
    public MetadataFull updateFolderMetadata(String folderId, String scope, String templateKey, List<FileMetadataUpdateRequest> body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'api';
        request.path = '/' + 'folders' + '/' + folderId + '/' + 'metadata' + '/' + scope + '/' + templateKey;
        Object wireBody = JSON.deserializeUntyped(JSON.serialize(body, true));
            if (wireBody instanceof List<Object>) {
                List<Object> wLst0 = (List<Object>) wireBody;
                for (Integer wIdx0 = 0; wIdx0 < wLst0.size(); wIdx0++) {
                    Object wElem0 = wLst0[wIdx0];
            if (wElem0 instanceof Map<String, Object>) wElem0 = FileMetadataUpdateRequest.denormalizeKeys((Map<String, Object>) wElem0);
                    wLst0[wIdx0] = wElem0;
                }
            }
        request.body = wireBody;
        request.suppressNulls = false;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = MetadataFull.normalizeKeys((Map<String, Object>) parsed);
        return (MetadataFull) JSON.deserialize(JSON.serialize(parsed), MetadataFull.class);
    }

    /**
     * `POST /folders/{folder_id}/metadata/{scope}/{template_key}`
     *
     * @param folderId path parameter
     * @param scope path parameter
     * @param templateKey path parameter
     * @param body the request body (`Map<String, Object>`)
     * @return `MetadataFull`
     */
    public MetadataFull createFolderMetadata(String folderId, String scope, String templateKey, Map<String, Object> body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'folders' + '/' + folderId + '/' + 'metadata' + '/' + scope + '/' + templateKey;
        request.body = body;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = MetadataFull.normalizeKeys((Map<String, Object>) parsed);
        return (MetadataFull) JSON.deserialize(JSON.serialize(parsed), MetadataFull.class);
    }

    /**
     * `DELETE /folders/{folder_id}/metadata/{scope}/{template_key}`
     *
     * @param folderId path parameter
     * @param scope path parameter
     * @param templateKey path parameter
     */
    public void deleteFolderMetadata(String folderId, String scope, String templateKey) {
        BoxRequest request = new BoxRequest();
        request.method = 'DELETE';
        request.baseUrl = 'api';
        request.path = '/' + 'folders' + '/' + folderId + '/' + 'metadata' + '/' + scope + '/' + templateKey;
        BoxResponse response = client.send(request);
    }
}
