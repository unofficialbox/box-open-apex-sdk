// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `AiExtractStructured` schema (7 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class AiExtractStructured {
    public List<AiItemBase> items; // wire: items
    public AiExtractStructuredMetadataTemplate metadata_template; // wire: metadata_template
    public List<AiExtractStructuredFields> fields; // wire: fields
    public Object ai_agent; // wire: ai_agent
    public Boolean include_confidence_score; // wire: include_confidence_score
    public Boolean include_reference; // wire: include_reference
    public List<Object> taxonomy_sources; // wire: taxonomy_sources
    /** Build `AiExtractStructured` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON) or an open extra bag, so those are detached, the
     * typed shell is deserialized, then they are reattached from the
     * raw tree (D-140). */
    public static AiExtractStructured deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_ai_agent = raw.remove('ai_agent');
        Object d_taxonomy_sources = raw.remove('taxonomy_sources');
        AiExtractStructured result = (AiExtractStructured) JSON.deserialize(JSON.serialize(raw), AiExtractStructured.class);
        result.ai_agent = d_ai_agent;
        result.taxonomy_sources = (List<Object>) d_taxonomy_sources;
        return result;
    }
}
