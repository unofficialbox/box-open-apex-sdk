// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `EnterpriseConfiguration` schema (6 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields rename between the wire and Apex, so this class carries
 * `normalizeKeys`/`denormalizeKeys` (used by the managers) to remap
 * JSON keys around native (de)serialization. */
public class EnterpriseConfiguration {
    public String id; // wire: id
    public String type; // wire: type
    public EnterpriseConfigurationSecurity security; // wire: security
    public EnterpriseConfigurationContentAndSharing content_and_sharing; // wire: content_and_sharing
    public EnterpriseConfigurationUserSettings user_settings; // wire: user_settings
    public EnterpriseConfigurationShield shield; // wire: shield
    /** Wire → Apex: JSON keys for the `EnterpriseConfiguration` wire shape (see class doc). */
    public static Map<String, Object> normalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('security')) {
            Object v = raw.remove('security');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationSecurity.normalizeKeys((Map<String, Object>) v);
            raw.put('security', v);
        }
        if (raw.containsKey('content_and_sharing')) {
            Object v = raw.remove('content_and_sharing');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationContentAndSharing.normalizeKeys((Map<String, Object>) v);
            raw.put('content_and_sharing', v);
        }
        if (raw.containsKey('user_settings')) {
            Object v = raw.remove('user_settings');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationUserSettings.normalizeKeys((Map<String, Object>) v);
            raw.put('user_settings', v);
        }
        return raw;
    }
    /** Apex → wire: JSON keys for the `EnterpriseConfiguration` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('security')) {
            Object v = raw.remove('security');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationSecurity.denormalizeKeys((Map<String, Object>) v);
            raw.put('security', v);
        }
        if (raw.containsKey('content_and_sharing')) {
            Object v = raw.remove('content_and_sharing');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationContentAndSharing.denormalizeKeys((Map<String, Object>) v);
            raw.put('content_and_sharing', v);
        }
        if (raw.containsKey('user_settings')) {
            Object v = raw.remove('user_settings');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationUserSettings.denormalizeKeys((Map<String, Object>) v);
            raw.put('user_settings', v);
        }
        return raw;
    }
}
