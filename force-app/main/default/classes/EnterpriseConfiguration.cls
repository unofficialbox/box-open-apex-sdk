// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `EnterpriseConfiguration` schema (6 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class EnterpriseConfiguration {
    public String id; // wire: id
    public String type; // wire: type
    public Object security; // wire: security
    public Object content_and_sharing; // wire: content_and_sharing
    public Object user_settings; // wire: user_settings
    public Object shield; // wire: shield
    /** Build `EnterpriseConfiguration` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON), so those are detached, the typed shell is
     * deserialized, then they are reattached from the raw tree (D-140). */
    public static EnterpriseConfiguration deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_security = raw.remove('security');
        Object d_content_and_sharing = raw.remove('content_and_sharing');
        Object d_user_settings = raw.remove('user_settings');
        Object d_shield = raw.remove('shield');
        EnterpriseConfiguration result = (EnterpriseConfiguration) JSON.deserialize(JSON.serialize(raw), EnterpriseConfiguration.class);
        result.security = d_security;
        result.content_and_sharing = d_content_and_sharing;
        result.user_settings = d_user_settings;
        result.shield = d_shield;
        return result;
    }
}
