// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `SignRequestSigner` schema (19 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class SignRequestSigner {
    public String email; // wire: email
    public String role; // wire: role
    public Boolean is_in_person; // wire: is_in_person
    public Long order; // wire: order
    public String embed_url_external_user_id; // wire: embed_url_external_user_id
    public String redirect_url; // wire: redirect_url
    public String declined_redirect_url; // wire: declined_redirect_url
    public Boolean login_required; // wire: login_required
    public String verification_phone_number; // wire: verification_phone_number
    public String password; // wire: password
    public String signer_group_id; // wire: signer_group_id
    public Boolean suppress_notifications; // wire: suppress_notifications
    public String language; // wire: language
    public Boolean has_viewed_document; // wire: has_viewed_document
    public SignRequestSignerSignerDecision signer_decision; // wire: signer_decision
    public List<SignRequestSignerInput> inputs; // wire: inputs
    public String embed_url; // wire: embed_url
    public String iframeable_embed_url; // wire: iframeable_embed_url
    public List<SignRequestSignerAttachment> attachments; // wire: attachments
    /** Build `SignRequestSigner` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON), so those are detached, the typed shell is
     * deserialized, then they are reattached from the raw tree (D-140). */
    public static SignRequestSigner deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_inputs = raw.remove('inputs');
        SignRequestSigner result = (SignRequestSigner) JSON.deserialize(JSON.serialize(raw), SignRequestSigner.class);
        if (d_inputs == null) {
            result.inputs = null;
        } else {
            List<SignRequestSignerInput> dLst0 = new List<SignRequestSignerInput>();
            for (Object dEl0 : (List<Object>) d_inputs) {
                SignRequestSignerInput dEv0;
        dEv0 = SignRequestSignerInput.deserialize(dEl0);
                dLst0.add(dEv0);
            }
            result.inputs = dLst0;
        }
        return result;
    }
}
