// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `files` resource manager — 5 operation(s).
 * Reached through the `Box` client (`client.files`), never
 * constructed directly.
 */
public with sharing class BoxFiles {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxFiles(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /files/{file_id}`
     *
     * @param fileId path parameter
     * @param fields query parameter, optional
     * @param ifNoneMatch header parameter, optional
     * @param boxapi header parameter, optional
     * @param xRepHints header parameter, optional
     * @return `FileFull`
     */
    public FileFull get(String fileId, List<String> fields, String ifNoneMatch, String boxapi, String xRepHints) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        if (ifNoneMatch != null) request.headers.put('if-none-match', String.valueOf(ifNoneMatch));
        if (boxapi != null) request.headers.put('boxapi', String.valueOf(boxapi));
        if (xRepHints != null) request.headers.put('x-rep-hints', String.valueOf(xRepHints));
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        FileFull deserialized;
        deserialized = FileFull.deserialize(parsed);
        return deserialized;
    }

    /**
     * `PUT /files/{file_id}`
     *
     * @param fileId path parameter
     * @param fields query parameter, optional
     * @param ifMatch header parameter, optional
     * @param body the request body (`UpdateFileRequest`)
     * @return `FileFull`
     */
    public FileFull update_r(String fileId, List<String> fields, String ifMatch, UpdateFileRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        if (ifMatch != null) request.headers.put('if-match', String.valueOf(ifMatch));
        Object wireBody = JSON.deserializeUntyped(JSON.serialize(body, true));
            if (wireBody instanceof Map<String, Object>) wireBody = UpdateFileRequest.denormalizeKeys((Map<String, Object>) wireBody);
        request.body = wireBody;
        request.suppressNulls = false;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        FileFull deserialized;
        deserialized = FileFull.deserialize(parsed);
        return deserialized;
    }

    /**
     * `DELETE /files/{file_id}`
     *
     * @param fileId path parameter
     * @param ifMatch header parameter, optional
     */
    public void delete_r(String fileId, String ifMatch) {
        BoxRequest request = new BoxRequest();
        request.method = 'DELETE';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId;
        if (ifMatch != null) request.headers.put('if-match', String.valueOf(ifMatch));
        BoxResponse response = client.send(request);
    }

    /**
     * `POST /files/{file_id}/copy`
     *
     * @param fileId path parameter
     * @param fields query parameter, optional
     * @param body the request body (`CopyFileRequest`)
     * @return `FileFull`
     */
    public FileFull copy(String fileId, List<String> fields, CopyFileRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId + '/' + 'copy';
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        request.body = body;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        FileFull deserialized;
        deserialized = FileFull.deserialize(parsed);
        return deserialized;
    }

    /**
     * `GET /files/{file_id}/thumbnail.{extension}`
     *
     * @param fileId path parameter
     * @param extension path parameter
     * @param minHeight query parameter, optional
     * @param minWidth query parameter, optional
     * @param maxHeight query parameter, optional
     * @param maxWidth query parameter, optional
     * @return `Blob`
     */
    public Blob getThumbnail(String fileId, String extension, Long minHeight, Long minWidth, Long maxHeight, Long maxWidth) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId + '/' + 'thumbnail.' + extension;
        if (minHeight != null) request.query.put('min_height', String.valueOf(minHeight));
        if (minWidth != null) request.query.put('min_width', String.valueOf(minWidth));
        if (maxHeight != null) request.query.put('max_height', String.valueOf(maxHeight));
        if (maxWidth != null) request.query.put('max_width', String.valueOf(maxWidth));
        BoxResponse response = client.send(request);
        return response.binaryBody;
    }
}
