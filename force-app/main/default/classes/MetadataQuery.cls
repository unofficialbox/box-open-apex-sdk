// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `MetadataQuery` schema (8 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields rename between the wire and Apex, so this class carries
 * `normalizeKeys`/`denormalizeKeys` (used by the managers) to remap
 * JSON keys around native (de)serialization.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class MetadataQuery {
    public String from_r; // wire: from
    public String query; // wire: query
    public Map<String, Object> query_params; // wire: query_params
    public String ancestor_folder_id; // wire: ancestor_folder_id
    public List<MetadataQueryOrderBy> order_by; // wire: order_by
    public Long limit_r; // wire: limit
    public String marker; // wire: marker
    public List<String> fields; // wire: fields
    /** Wire → Apex: JSON keys for the `MetadataQuery` wire shape (see class doc). */
    public static Map<String, Object> normalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('from')) {
            Object v = raw.remove('from');
            raw.put('from_r', v);
        }
        if (raw.containsKey('limit')) {
            Object v = raw.remove('limit');
            raw.put('limit_r', v);
        }
        return raw;
    }
    /** Apex → wire: JSON keys for the `MetadataQuery` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('from_r')) {
            Object v = raw.remove('from_r');
            raw.put('from', v);
        }
        if (raw.containsKey('limit_r')) {
            Object v = raw.remove('limit_r');
            raw.put('limit', v);
        }
        return raw;
    }
    /** Build `MetadataQuery` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON), so those are detached, the typed shell is
     * deserialized, then they are reattached from the raw tree (D-140). */
    public static MetadataQuery deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_query_params = raw.remove('query_params');
        MetadataQuery result = (MetadataQuery) JSON.deserialize(JSON.serialize(MetadataQuery.normalizeKeys(raw)), MetadataQuery.class);
        result.query_params = (Map<String, Object>) d_query_params;
        return result;
    }
}
