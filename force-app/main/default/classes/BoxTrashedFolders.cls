// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `trashed_folders` resource manager — 3 operation(s).
 * Reached through the `Box` client (`client.trashedFolders`), never
 * constructed directly.
 */
public with sharing class BoxTrashedFolders {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxTrashedFolders(BoxClient client) {
        this.client = client;
    }

    /**
     * `POST /folders/{folder_id}`
     *
     * @param folderId path parameter
     * @param fields query parameter, optional
     * @param body the request body (`FileCreateRequest`)
     * @return `TrashFolderRestored`
     */
    public TrashFolderRestored createFolder(String folderId, List<String> fields, FileCreateRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'folders' + '/' + folderId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        request.body = body;
        BoxResponse response = client.send(request);
        return (TrashFolderRestored) JSON.deserialize(response.body, TrashFolderRestored.class);
    }

    /**
     * `GET /folders/{folder_id}/trash`
     *
     * @param folderId path parameter
     * @param fields query parameter, optional
     * @return `TrashFolder`
     */
    public TrashFolder getFolderTrash(String folderId, List<String> fields) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'folders' + '/' + folderId + '/' + 'trash';
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        BoxResponse response = client.send(request);
        return (TrashFolder) JSON.deserialize(response.body, TrashFolder.class);
    }

    /**
     * `DELETE /folders/{folder_id}/trash`
     *
     * @param folderId path parameter
     */
    public void deleteFolderTrash(String folderId) {
        BoxRequest request = new BoxRequest();
        request.method = 'DELETE';
        request.baseUrl = 'api';
        request.path = '/' + 'folders' + '/' + folderId + '/' + 'trash';
        BoxResponse response = client.send(request);
    }
}
