// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `invites` resource manager — 2 operation(s).
 * Reached through the `Box` client (`client.invites`), never
 * constructed directly.
 */
public with sharing class BoxInvites {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxInvites(BoxClient client) {
        this.client = client;
    }

    /**
     * `POST /invites`
     *
     * @param fields query parameter, optional
     * @param body the request body (`InviteCreateRequest`)
     * @return `Invite`
     */
    public Invite create(List<String> fields, InviteCreateRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'invites';
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        request.body = body;
        BoxResponse response = client.send(request);
        return (Invite) JSON.deserialize(response.body, Invite.class);
    }

    /**
     * `GET /invites/{invite_id}`
     *
     * @param inviteId path parameter
     * @param fields query parameter, optional
     * @return `Invite`
     */
    public Invite get(String inviteId, List<String> fields) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'invites' + '/' + inviteId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        BoxResponse response = client.send(request);
        return (Invite) JSON.deserialize(response.body, Invite.class);
    }
}
