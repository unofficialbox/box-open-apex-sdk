// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `ClientError` schema (7 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class ClientError {
    public String type; // wire: type
    public Long status; // wire: status
    public String code; // wire: code
    public String message; // wire: message
    public Map<String, Object> context_info; // wire: context_info
    public String help_url; // wire: help_url
    public String request_id; // wire: request_id
    /** Build `ClientError` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON) or an open extra bag, so those are detached, the
     * typed shell is deserialized, then they are reattached from the
     * raw tree (D-140). */
    public static ClientError deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_context_info = raw.remove('context_info');
        ClientError result = (ClientError) JSON.deserialize(JSON.serialize(raw), ClientError.class);
        result.context_info = (Map<String, Object>) d_context_info;
        return result;
    }
}
