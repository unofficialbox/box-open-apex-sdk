// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `EnterpriseConfigurationUserSettings` schema (19 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields rename between the wire and Apex, so this class carries
 * `normalizeKeys`/`denormalizeKeys` (used by the managers) to remap
 * JSON keys around native (de)serialization. */
public class EnterpriseConfigurationUserSettings {
    public List<EnterpriseFeatureSettingsItem> enterprise_feature_settings; // wire: enterprise_feature_settings
    public EnterpriseConfigurationItemString user_invites_expiration_time_frame; // wire: user_invites_expiration_time_frame
    public EnterpriseConfigurationItemBoolean is_username_change_restricted; // wire: is_username_change_restricted
    public EnterpriseConfigurationItemBoolean is_box_sync_restricted_for_new_users; // wire: is_box_sync_restricted_for_new_users
    public EnterpriseConfigurationItemBoolean is_view_all_users_enabled_for_new_users; // wire: is_view_all_users_enabled_for_new_users
    public EnterpriseConfigurationItemBoolean is_device_limit_exemption_enabled_for_new_users; // wire: is_device_limit_exemption_enabled_for_new_users
    public EnterpriseConfigurationItemBoolean is_external_collaboration_restricted_for_new_users; // wire: is_external_collaboration_restricted_for_new_users
    public EnterpriseConfigurationItemBoolean is_unlimited_storage_enabled_for_new_users; // wire: is_unlimited_storage_enabled_for_new_users
    public EnterpriseConfigurationItemInteger new_user_default_storage_limit; // wire: new_user_default_storage_limit
    public EnterpriseConfigurationItemString new_user_default_timezone; // wire: new_user_default_timezone
    public EnterpriseConfigurationItemString new_user_default_language; // wire: new_user_default_language
    public EnterpriseConfigurationItemBoolean is_enterprise_sso_required; // wire: is_enterprise_sso_required
    public EnterpriseConfigurationItemBoolean is_enterprise_sso_in_testing; // wire: is_enterprise_sso_in_testing
    public EnterpriseConfigurationItemBoolean is_sso_auto_add_groups_enabled; // wire: is_sso_auto_add_groups_enabled
    public EnterpriseConfigurationItemBoolean is_sso_auto_add_user_to_groups_enabled; // wire: is_sso_auto_add_user_to_groups_enabled
    public EnterpriseConfigurationItemBoolean is_sso_auto_remove_user_from_groups_enabled; // wire: is_sso_auto_remove_user_from_groups_enabled
    public EnterpriseConfigurationUserSetti_a6bf382 user_tracking_codes; // wire: user_tracking_codes
    public EnterpriseConfigurationItemInteger number_of_user_tracking_codes_remaining; // wire: number_of_user_tracking_codes_remaining
    public EnterpriseConfigurationItemBoolean is_instant_login_restricted; // wire: is_instant_login_restricted
    /** Wire → Apex: JSON keys for the `EnterpriseConfigurationUserSettings` wire shape (see class doc). */
    public static Map<String, Object> normalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('enterprise_feature_settings')) {
            Object v = raw.remove('enterprise_feature_settings');
            if (v instanceof List<Object>) {
                List<Object> wLst0 = (List<Object>) v;
                for (Integer wIdx0 = 0; wIdx0 < wLst0.size(); wIdx0++) {
                    Object wElem0 = wLst0[wIdx0];
            if (wElem0 instanceof Map<String, Object>) wElem0 = EnterpriseFeatureSettingsItem.normalizeKeys((Map<String, Object>) wElem0);
                    wLst0[wIdx0] = wElem0;
                }
            }
            raw.put('enterprise_feature_settings', v);
        }
        if (raw.containsKey('user_invites_expiration_time_frame')) {
            Object v = raw.remove('user_invites_expiration_time_frame');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemString.normalizeKeys((Map<String, Object>) v);
            raw.put('user_invites_expiration_time_frame', v);
        }
        if (raw.containsKey('is_username_change_restricted')) {
            Object v = raw.remove('is_username_change_restricted');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.normalizeKeys((Map<String, Object>) v);
            raw.put('is_username_change_restricted', v);
        }
        if (raw.containsKey('is_box_sync_restricted_for_new_users')) {
            Object v = raw.remove('is_box_sync_restricted_for_new_users');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.normalizeKeys((Map<String, Object>) v);
            raw.put('is_box_sync_restricted_for_new_users', v);
        }
        if (raw.containsKey('is_view_all_users_enabled_for_new_users')) {
            Object v = raw.remove('is_view_all_users_enabled_for_new_users');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.normalizeKeys((Map<String, Object>) v);
            raw.put('is_view_all_users_enabled_for_new_users', v);
        }
        if (raw.containsKey('is_device_limit_exemption_enabled_for_new_users')) {
            Object v = raw.remove('is_device_limit_exemption_enabled_for_new_users');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.normalizeKeys((Map<String, Object>) v);
            raw.put('is_device_limit_exemption_enabled_for_new_users', v);
        }
        if (raw.containsKey('is_external_collaboration_restricted_for_new_users')) {
            Object v = raw.remove('is_external_collaboration_restricted_for_new_users');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.normalizeKeys((Map<String, Object>) v);
            raw.put('is_external_collaboration_restricted_for_new_users', v);
        }
        if (raw.containsKey('is_unlimited_storage_enabled_for_new_users')) {
            Object v = raw.remove('is_unlimited_storage_enabled_for_new_users');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.normalizeKeys((Map<String, Object>) v);
            raw.put('is_unlimited_storage_enabled_for_new_users', v);
        }
        if (raw.containsKey('new_user_default_storage_limit')) {
            Object v = raw.remove('new_user_default_storage_limit');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemInteger.normalizeKeys((Map<String, Object>) v);
            raw.put('new_user_default_storage_limit', v);
        }
        if (raw.containsKey('new_user_default_timezone')) {
            Object v = raw.remove('new_user_default_timezone');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemString.normalizeKeys((Map<String, Object>) v);
            raw.put('new_user_default_timezone', v);
        }
        if (raw.containsKey('new_user_default_language')) {
            Object v = raw.remove('new_user_default_language');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemString.normalizeKeys((Map<String, Object>) v);
            raw.put('new_user_default_language', v);
        }
        if (raw.containsKey('is_enterprise_sso_required')) {
            Object v = raw.remove('is_enterprise_sso_required');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.normalizeKeys((Map<String, Object>) v);
            raw.put('is_enterprise_sso_required', v);
        }
        if (raw.containsKey('is_enterprise_sso_in_testing')) {
            Object v = raw.remove('is_enterprise_sso_in_testing');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.normalizeKeys((Map<String, Object>) v);
            raw.put('is_enterprise_sso_in_testing', v);
        }
        if (raw.containsKey('is_sso_auto_add_groups_enabled')) {
            Object v = raw.remove('is_sso_auto_add_groups_enabled');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.normalizeKeys((Map<String, Object>) v);
            raw.put('is_sso_auto_add_groups_enabled', v);
        }
        if (raw.containsKey('is_sso_auto_add_user_to_groups_enabled')) {
            Object v = raw.remove('is_sso_auto_add_user_to_groups_enabled');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.normalizeKeys((Map<String, Object>) v);
            raw.put('is_sso_auto_add_user_to_groups_enabled', v);
        }
        if (raw.containsKey('is_sso_auto_remove_user_from_groups_enabled')) {
            Object v = raw.remove('is_sso_auto_remove_user_from_groups_enabled');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.normalizeKeys((Map<String, Object>) v);
            raw.put('is_sso_auto_remove_user_from_groups_enabled', v);
        }
        if (raw.containsKey('user_tracking_codes')) {
            Object v = raw.remove('user_tracking_codes');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationUserSetti_a6bf382.normalizeKeys((Map<String, Object>) v);
            raw.put('user_tracking_codes', v);
        }
        if (raw.containsKey('number_of_user_tracking_codes_remaining')) {
            Object v = raw.remove('number_of_user_tracking_codes_remaining');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemInteger.normalizeKeys((Map<String, Object>) v);
            raw.put('number_of_user_tracking_codes_remaining', v);
        }
        if (raw.containsKey('is_instant_login_restricted')) {
            Object v = raw.remove('is_instant_login_restricted');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.normalizeKeys((Map<String, Object>) v);
            raw.put('is_instant_login_restricted', v);
        }
        return raw;
    }
    /** Apex → wire: JSON keys for the `EnterpriseConfigurationUserSettings` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('enterprise_feature_settings')) {
            Object v = raw.remove('enterprise_feature_settings');
            if (v instanceof List<Object>) {
                List<Object> wLst0 = (List<Object>) v;
                for (Integer wIdx0 = 0; wIdx0 < wLst0.size(); wIdx0++) {
                    Object wElem0 = wLst0[wIdx0];
            if (wElem0 instanceof Map<String, Object>) wElem0 = EnterpriseFeatureSettingsItem.denormalizeKeys((Map<String, Object>) wElem0);
                    wLst0[wIdx0] = wElem0;
                }
            }
            raw.put('enterprise_feature_settings', v);
        }
        if (raw.containsKey('user_invites_expiration_time_frame')) {
            Object v = raw.remove('user_invites_expiration_time_frame');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemString.denormalizeKeys((Map<String, Object>) v);
            raw.put('user_invites_expiration_time_frame', v);
        }
        if (raw.containsKey('is_username_change_restricted')) {
            Object v = raw.remove('is_username_change_restricted');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.denormalizeKeys((Map<String, Object>) v);
            raw.put('is_username_change_restricted', v);
        }
        if (raw.containsKey('is_box_sync_restricted_for_new_users')) {
            Object v = raw.remove('is_box_sync_restricted_for_new_users');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.denormalizeKeys((Map<String, Object>) v);
            raw.put('is_box_sync_restricted_for_new_users', v);
        }
        if (raw.containsKey('is_view_all_users_enabled_for_new_users')) {
            Object v = raw.remove('is_view_all_users_enabled_for_new_users');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.denormalizeKeys((Map<String, Object>) v);
            raw.put('is_view_all_users_enabled_for_new_users', v);
        }
        if (raw.containsKey('is_device_limit_exemption_enabled_for_new_users')) {
            Object v = raw.remove('is_device_limit_exemption_enabled_for_new_users');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.denormalizeKeys((Map<String, Object>) v);
            raw.put('is_device_limit_exemption_enabled_for_new_users', v);
        }
        if (raw.containsKey('is_external_collaboration_restricted_for_new_users')) {
            Object v = raw.remove('is_external_collaboration_restricted_for_new_users');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.denormalizeKeys((Map<String, Object>) v);
            raw.put('is_external_collaboration_restricted_for_new_users', v);
        }
        if (raw.containsKey('is_unlimited_storage_enabled_for_new_users')) {
            Object v = raw.remove('is_unlimited_storage_enabled_for_new_users');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.denormalizeKeys((Map<String, Object>) v);
            raw.put('is_unlimited_storage_enabled_for_new_users', v);
        }
        if (raw.containsKey('new_user_default_storage_limit')) {
            Object v = raw.remove('new_user_default_storage_limit');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemInteger.denormalizeKeys((Map<String, Object>) v);
            raw.put('new_user_default_storage_limit', v);
        }
        if (raw.containsKey('new_user_default_timezone')) {
            Object v = raw.remove('new_user_default_timezone');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemString.denormalizeKeys((Map<String, Object>) v);
            raw.put('new_user_default_timezone', v);
        }
        if (raw.containsKey('new_user_default_language')) {
            Object v = raw.remove('new_user_default_language');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemString.denormalizeKeys((Map<String, Object>) v);
            raw.put('new_user_default_language', v);
        }
        if (raw.containsKey('is_enterprise_sso_required')) {
            Object v = raw.remove('is_enterprise_sso_required');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.denormalizeKeys((Map<String, Object>) v);
            raw.put('is_enterprise_sso_required', v);
        }
        if (raw.containsKey('is_enterprise_sso_in_testing')) {
            Object v = raw.remove('is_enterprise_sso_in_testing');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.denormalizeKeys((Map<String, Object>) v);
            raw.put('is_enterprise_sso_in_testing', v);
        }
        if (raw.containsKey('is_sso_auto_add_groups_enabled')) {
            Object v = raw.remove('is_sso_auto_add_groups_enabled');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.denormalizeKeys((Map<String, Object>) v);
            raw.put('is_sso_auto_add_groups_enabled', v);
        }
        if (raw.containsKey('is_sso_auto_add_user_to_groups_enabled')) {
            Object v = raw.remove('is_sso_auto_add_user_to_groups_enabled');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.denormalizeKeys((Map<String, Object>) v);
            raw.put('is_sso_auto_add_user_to_groups_enabled', v);
        }
        if (raw.containsKey('is_sso_auto_remove_user_from_groups_enabled')) {
            Object v = raw.remove('is_sso_auto_remove_user_from_groups_enabled');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.denormalizeKeys((Map<String, Object>) v);
            raw.put('is_sso_auto_remove_user_from_groups_enabled', v);
        }
        if (raw.containsKey('user_tracking_codes')) {
            Object v = raw.remove('user_tracking_codes');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationUserSetti_a6bf382.denormalizeKeys((Map<String, Object>) v);
            raw.put('user_tracking_codes', v);
        }
        if (raw.containsKey('number_of_user_tracking_codes_remaining')) {
            Object v = raw.remove('number_of_user_tracking_codes_remaining');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemInteger.denormalizeKeys((Map<String, Object>) v);
            raw.put('number_of_user_tracking_codes_remaining', v);
        }
        if (raw.containsKey('is_instant_login_restricted')) {
            Object v = raw.remove('is_instant_login_restricted');
            if (v instanceof Map<String, Object>) v = EnterpriseConfigurationItemBoolean.denormalizeKeys((Map<String, Object>) v);
            raw.put('is_instant_login_restricted', v);
        }
        return raw;
    }
}
