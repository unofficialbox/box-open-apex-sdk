// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `DevicePinners` schema (4 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields rename between the wire and Apex, so this class carries
 * `normalizeKeys`/`denormalizeKeys` (used by the managers) to remap
 * JSON keys around native (de)serialization. */
public class DevicePinners {
    public List<DevicePinner> entries; // wire: entries
    public Long limit_r; // wire: limit
    public Long next_marker; // wire: next_marker
    public List<DevicePinnersOrder> order; // wire: order
    /** Wire → Apex: JSON keys for the `DevicePinners` wire shape (see class doc). */
    public static Map<String, Object> normalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('limit')) {
            Object v = raw.remove('limit');
            raw.put('limit_r', v);
        }
        if (raw.containsKey('order')) {
            Object v = raw.remove('order');
            if (v instanceof List<Object>) {
                List<Object> wLst0 = (List<Object>) v;
                for (Integer wIdx0 = 0; wIdx0 < wLst0.size(); wIdx0++) {
                    Object wElem0 = wLst0[wIdx0];
            if (wElem0 instanceof Map<String, Object>) wElem0 = DevicePinnersOrder.normalizeKeys((Map<String, Object>) wElem0);
                    wLst0[wIdx0] = wElem0;
                }
            }
            raw.put('order', v);
        }
        return raw;
    }
    /** Apex → wire: JSON keys for the `DevicePinners` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('limit_r')) {
            Object v = raw.remove('limit_r');
            raw.put('limit', v);
        }
        if (raw.containsKey('order')) {
            Object v = raw.remove('order');
            if (v instanceof List<Object>) {
                List<Object> wLst0 = (List<Object>) v;
                for (Integer wIdx0 = 0; wIdx0 < wLst0.size(); wIdx0++) {
                    Object wElem0 = wLst0[wIdx0];
            if (wElem0 instanceof Map<String, Object>) wElem0 = DevicePinnersOrder.denormalizeKeys((Map<String, Object>) wElem0);
                    wLst0[wIdx0] = wElem0;
                }
            }
            raw.put('order', v);
        }
        return raw;
    }
}
