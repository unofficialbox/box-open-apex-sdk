// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `TimelineSkillCard` schema (8 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields rename between the wire and Apex, so this class carries
 * `normalizeKeys`/`denormalizeKeys` (used by the managers) to remap
 * JSON keys around native (de)serialization. */
public class TimelineSkillCard {
    public Datetime created_at; // wire: created_at
    public String type; // wire: type
    public String skill_card_type; // wire: skill_card_type
    public KeywordSkillCardSkillCardTitle skill_card_title; // wire: skill_card_title
    public KeywordSkillCardSkill skill; // wire: skill
    public KeywordSkillCardInvocation invocation; // wire: invocation
    public Long duration; // wire: duration
    public List<TimelineSkillCardEntries> entries; // wire: entries
    /** Wire → Apex: JSON keys for the `TimelineSkillCard` wire shape (see class doc). */
    public static Map<String, Object> normalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('entries')) {
            Object v = raw.remove('entries');
            if (v instanceof List<Object>) {
                List<Object> wLst0 = (List<Object>) v;
                for (Integer wIdx0 = 0; wIdx0 < wLst0.size(); wIdx0++) {
                    Object wElem0 = wLst0[wIdx0];
            if (wElem0 instanceof Map<String, Object>) wElem0 = TimelineSkillCardEntries.normalizeKeys((Map<String, Object>) wElem0);
                    wLst0[wIdx0] = wElem0;
                }
            }
            raw.put('entries', v);
        }
        return raw;
    }
    /** Apex → wire: JSON keys for the `TimelineSkillCard` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('entries')) {
            Object v = raw.remove('entries');
            if (v instanceof List<Object>) {
                List<Object> wLst0 = (List<Object>) v;
                for (Integer wIdx0 = 0; wIdx0 < wLst0.size(); wIdx0++) {
                    Object wElem0 = wLst0[wIdx0];
            if (wElem0 instanceof Map<String, Object>) wElem0 = TimelineSkillCardEntries.denormalizeKeys((Map<String, Object>) wElem0);
                    wLst0[wIdx0] = wElem0;
                }
            }
            raw.put('entries', v);
        }
        return raw;
    }
}
