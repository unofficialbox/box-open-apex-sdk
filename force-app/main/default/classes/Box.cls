// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The Box SDK entry point: one field per resource manager.
 * Construct it with a `BoxClient` (the hand-written runtime that
 * performs auth + HTTP callouts), then reach an endpoint through its
 * manager, e.g. `new Box(client).files.getById(...)`.
 */
public with sharing class Box {
    /** The `ai` resource manager. */
    public final BoxAi ai;
    /** The `aiStudio` resource manager. */
    public final BoxAiStudio aiStudio;
    /** The `appItemAssociations` resource manager. */
    public final BoxAppItemAssociations appItemAssociations;
    /** The `archives` resource manager. */
    public final BoxArchives archives;
    /** The `authorization` resource manager. */
    public final BoxAuthorization authorization;
    /** The `automateWorkflows` resource manager. */
    public final BoxAutomateWorkflows automateWorkflows;
    /** The `avatars` resource manager. */
    public final BoxAvatars avatars;
    /** The `chunkedUploads` resource manager. */
    public final BoxChunkedUploads chunkedUploads;
    /** The `classifications` resource manager. */
    public final BoxClassifications classifications;
    /** The `collaborationAllowlistEntries` resource manager. */
    public final BoxCollaborationAllowlistEntries collaborationAllowlistEntries;
    /** The `collaborationAllowlistExemptTargets` resource manager. */
    public final BoxCollaborationAllowlistExemptTargets collaborationAllowlistExemptTargets;
    /** The `collections` resource manager. */
    public final BoxCollections collections;
    /** The `comments` resource manager. */
    public final BoxComments comments;
    /** The `devicePinners` resource manager. */
    public final BoxDevicePinners devicePinners;
    /** The `docgen` resource manager. */
    public final BoxDocgen docgen;
    /** The `docgenTemplate` resource manager. */
    public final BoxDocgenTemplate docgenTemplate;
    /** The `downloads` resource manager. */
    public final BoxDownloads downloads;
    /** The `emailAliases` resource manager. */
    public final BoxEmailAliases emailAliases;
    /** The `enterpriseConfigurations` resource manager. */
    public final BoxEnterpriseConfigurations enterpriseConfigurations;
    /** The `events` resource manager. */
    public final BoxEvents events;
    /** The `externalUsers` resource manager. */
    public final BoxExternalUsers externalUsers;
    /** The `fileClassifications` resource manager. */
    public final BoxFileClassifications fileClassifications;
    /** The `fileMetadata` resource manager. */
    public final BoxFileMetadata fileMetadata;
    /** The `fileRequests` resource manager. */
    public final BoxFileRequests fileRequests;
    /** The `fileVersionLegalHolds` resource manager. */
    public final BoxFileVersionLegalHolds fileVersionLegalHolds;
    /** The `fileVersionRetentions` resource manager. */
    public final BoxFileVersionRetentions fileVersionRetentions;
    /** The `fileVersions` resource manager. */
    public final BoxFileVersions fileVersions;
    /** The `fileWatermarks` resource manager. */
    public final BoxFileWatermarks fileWatermarks;
    /** The `files` resource manager. */
    public final BoxFiles files;
    /** The `folderClassifications` resource manager. */
    public final BoxFolderClassifications folderClassifications;
    /** The `folderLocks` resource manager. */
    public final BoxFolderLocks folderLocks;
    /** The `folderMetadata` resource manager. */
    public final BoxFolderMetadata folderMetadata;
    /** The `folderWatermarks` resource manager. */
    public final BoxFolderWatermarks folderWatermarks;
    /** The `folders` resource manager. */
    public final BoxFolders folders;
    /** The `groups` resource manager. */
    public final BoxGroups groups;
    /** The `hubCollaborations` resource manager. */
    public final BoxHubCollaborations hubCollaborations;
    /** The `hubDocument` resource manager. */
    public final BoxHubDocument hubDocument;
    /** The `hubItems` resource manager. */
    public final BoxHubItems hubItems;
    /** The `hubs` resource manager. */
    public final BoxHubs hubs;
    /** The `integrationMappings` resource manager. */
    public final BoxIntegrationMappings integrationMappings;
    /** The `invites` resource manager. */
    public final BoxInvites invites;
    /** The `legalHoldPolicies` resource manager. */
    public final BoxLegalHoldPolicies legalHoldPolicies;
    /** The `legalHoldPolicyAssignments` resource manager. */
    public final BoxLegalHoldPolicyAssignments legalHoldPolicyAssignments;
    /** The `listCollaborations` resource manager. */
    public final BoxListCollaborations listCollaborations;
    /** The `memberships` resource manager. */
    public final BoxMemberships memberships;
    /** The `metadataCascadePolicies` resource manager. */
    public final BoxMetadataCascadePolicies metadataCascadePolicies;
    /** The `metadataTaxonomies` resource manager. */
    public final BoxMetadataTaxonomies metadataTaxonomies;
    /** The `metadataTemplates` resource manager. */
    public final BoxMetadataTemplates metadataTemplates;
    /** The `notes` resource manager. */
    public final BoxNotes notes;
    /** The `recentItems` resource manager. */
    public final BoxRecentItems recentItems;
    /** The `retentionPolicies` resource manager. */
    public final BoxRetentionPolicies retentionPolicies;
    /** The `retentionPolicyAssignments` resource manager. */
    public final BoxRetentionPolicyAssignments retentionPolicyAssignments;
    /** The `search` resource manager. */
    public final BoxSearch search;
    /** The `sessionTermination` resource manager. */
    public final BoxSessionTermination sessionTermination;
    /** The `sharedLinksAppItems` resource manager. */
    public final BoxSharedLinksAppItems sharedLinksAppItems;
    /** The `sharedLinksFiles` resource manager. */
    public final BoxSharedLinksFiles sharedLinksFiles;
    /** The `sharedLinksFolders` resource manager. */
    public final BoxSharedLinksFolders sharedLinksFolders;
    /** The `sharedLinksWebLinks` resource manager. */
    public final BoxSharedLinksWebLinks sharedLinksWebLinks;
    /** The `shieldInformationBarrierReports` resource manager. */
    public final BoxShieldInformationBarrierReports shieldInformationBarrierReports;
    /** The `shieldInformationBarrierSegmentMembers` resource manager. */
    public final BoxShieldInformationBarrierSegme_6e664c6 shieldInformationBarrierSegmentMembers;
    /** The `shieldInformationBarrierSegmentRestrictions` resource manager. */
    public final BoxShieldInformationBarrierSegme_ccd386c shieldInformationBarrierSegmentRestrictions;
    /** The `shieldInformationBarrierSegments` resource manager. */
    public final BoxShieldInformationBarrierSegments shieldInformationBarrierSegments;
    /** The `shieldInformationBarriers` resource manager. */
    public final BoxShieldInformationBarriers shieldInformationBarriers;
    /** The `shieldLists` resource manager. */
    public final BoxShieldLists shieldLists;
    /** The `signRequests` resource manager. */
    public final BoxSignRequests signRequests;
    /** The `signTemplates` resource manager. */
    public final BoxSignTemplates signTemplates;
    /** The `skills` resource manager. */
    public final BoxSkills skills;
    /** The `storagePolicies` resource manager. */
    public final BoxStoragePolicies storagePolicies;
    /** The `storagePolicyAssignments` resource manager. */
    public final BoxStoragePolicyAssignments storagePolicyAssignments;
    /** The `taskAssignments` resource manager. */
    public final BoxTaskAssignments taskAssignments;
    /** The `tasks` resource manager. */
    public final BoxTasks tasks;
    /** The `termsOfServiceUserStatuses` resource manager. */
    public final BoxTermsOfServiceUserStatuses termsOfServiceUserStatuses;
    /** The `termsOfServices` resource manager. */
    public final BoxTermsOfServices termsOfServices;
    /** The `transfer` resource manager. */
    public final BoxTransfer transfer;
    /** The `trashedFiles` resource manager. */
    public final BoxTrashedFiles trashedFiles;
    /** The `trashedFolders` resource manager. */
    public final BoxTrashedFolders trashedFolders;
    /** The `trashedItems` resource manager. */
    public final BoxTrashedItems trashedItems;
    /** The `trashedWebLinks` resource manager. */
    public final BoxTrashedWebLinks trashedWebLinks;
    /** The `uploads` resource manager. */
    public final BoxUploads uploads;
    /** The `userCollaborations` resource manager. */
    public final BoxUserCollaborations userCollaborations;
    /** The `users` resource manager. */
    public final BoxUsers users;
    /** The `webLinks` resource manager. */
    public final BoxWebLinks webLinks;
    /** The `webhooks` resource manager. */
    public final BoxWebhooks webhooks;
    /** The `workflows` resource manager. */
    public final BoxWorkflows workflows;
    /** The `zipDownloads` resource manager. */
    public final BoxZipDownloads zipDownloads;
    /** Wire every manager to one shared runtime client. */
    public Box(BoxClient client) {
        this.ai = new BoxAi(client);
        this.aiStudio = new BoxAiStudio(client);
        this.appItemAssociations = new BoxAppItemAssociations(client);
        this.archives = new BoxArchives(client);
        this.authorization = new BoxAuthorization(client);
        this.automateWorkflows = new BoxAutomateWorkflows(client);
        this.avatars = new BoxAvatars(client);
        this.chunkedUploads = new BoxChunkedUploads(client);
        this.classifications = new BoxClassifications(client);
        this.collaborationAllowlistEntries = new BoxCollaborationAllowlistEntries(client);
        this.collaborationAllowlistExemptTargets = new BoxCollaborationAllowlistExemptTargets(client);
        this.collections = new BoxCollections(client);
        this.comments = new BoxComments(client);
        this.devicePinners = new BoxDevicePinners(client);
        this.docgen = new BoxDocgen(client);
        this.docgenTemplate = new BoxDocgenTemplate(client);
        this.downloads = new BoxDownloads(client);
        this.emailAliases = new BoxEmailAliases(client);
        this.enterpriseConfigurations = new BoxEnterpriseConfigurations(client);
        this.events = new BoxEvents(client);
        this.externalUsers = new BoxExternalUsers(client);
        this.fileClassifications = new BoxFileClassifications(client);
        this.fileMetadata = new BoxFileMetadata(client);
        this.fileRequests = new BoxFileRequests(client);
        this.fileVersionLegalHolds = new BoxFileVersionLegalHolds(client);
        this.fileVersionRetentions = new BoxFileVersionRetentions(client);
        this.fileVersions = new BoxFileVersions(client);
        this.fileWatermarks = new BoxFileWatermarks(client);
        this.files = new BoxFiles(client);
        this.folderClassifications = new BoxFolderClassifications(client);
        this.folderLocks = new BoxFolderLocks(client);
        this.folderMetadata = new BoxFolderMetadata(client);
        this.folderWatermarks = new BoxFolderWatermarks(client);
        this.folders = new BoxFolders(client);
        this.groups = new BoxGroups(client);
        this.hubCollaborations = new BoxHubCollaborations(client);
        this.hubDocument = new BoxHubDocument(client);
        this.hubItems = new BoxHubItems(client);
        this.hubs = new BoxHubs(client);
        this.integrationMappings = new BoxIntegrationMappings(client);
        this.invites = new BoxInvites(client);
        this.legalHoldPolicies = new BoxLegalHoldPolicies(client);
        this.legalHoldPolicyAssignments = new BoxLegalHoldPolicyAssignments(client);
        this.listCollaborations = new BoxListCollaborations(client);
        this.memberships = new BoxMemberships(client);
        this.metadataCascadePolicies = new BoxMetadataCascadePolicies(client);
        this.metadataTaxonomies = new BoxMetadataTaxonomies(client);
        this.metadataTemplates = new BoxMetadataTemplates(client);
        this.notes = new BoxNotes(client);
        this.recentItems = new BoxRecentItems(client);
        this.retentionPolicies = new BoxRetentionPolicies(client);
        this.retentionPolicyAssignments = new BoxRetentionPolicyAssignments(client);
        this.search = new BoxSearch(client);
        this.sessionTermination = new BoxSessionTermination(client);
        this.sharedLinksAppItems = new BoxSharedLinksAppItems(client);
        this.sharedLinksFiles = new BoxSharedLinksFiles(client);
        this.sharedLinksFolders = new BoxSharedLinksFolders(client);
        this.sharedLinksWebLinks = new BoxSharedLinksWebLinks(client);
        this.shieldInformationBarrierReports = new BoxShieldInformationBarrierReports(client);
        this.shieldInformationBarrierSegmentMembers = new BoxShieldInformationBarrierSegme_6e664c6(client);
        this.shieldInformationBarrierSegmentRestrictions = new BoxShieldInformationBarrierSegme_ccd386c(client);
        this.shieldInformationBarrierSegments = new BoxShieldInformationBarrierSegments(client);
        this.shieldInformationBarriers = new BoxShieldInformationBarriers(client);
        this.shieldLists = new BoxShieldLists(client);
        this.signRequests = new BoxSignRequests(client);
        this.signTemplates = new BoxSignTemplates(client);
        this.skills = new BoxSkills(client);
        this.storagePolicies = new BoxStoragePolicies(client);
        this.storagePolicyAssignments = new BoxStoragePolicyAssignments(client);
        this.taskAssignments = new BoxTaskAssignments(client);
        this.tasks = new BoxTasks(client);
        this.termsOfServiceUserStatuses = new BoxTermsOfServiceUserStatuses(client);
        this.termsOfServices = new BoxTermsOfServices(client);
        this.transfer = new BoxTransfer(client);
        this.trashedFiles = new BoxTrashedFiles(client);
        this.trashedFolders = new BoxTrashedFolders(client);
        this.trashedItems = new BoxTrashedItems(client);
        this.trashedWebLinks = new BoxTrashedWebLinks(client);
        this.uploads = new BoxUploads(client);
        this.userCollaborations = new BoxUserCollaborations(client);
        this.users = new BoxUsers(client);
        this.webLinks = new BoxWebLinks(client);
        this.webhooks = new BoxWebhooks(client);
        this.workflows = new BoxWorkflows(client);
        this.zipDownloads = new BoxZipDownloads(client);
    }
}
