// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `AiSingleAgentResponse` schema (11 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class AiSingleAgentResponse {
    public String id; // wire: id
    public String type; // wire: type
    public String origin; // wire: origin
    public String name; // wire: name
    public String access_state; // wire: access_state
    public UserBase created_by; // wire: created_by
    public Datetime created_at; // wire: created_at
    public UserBase modified_by; // wire: modified_by
    public Datetime modified_at; // wire: modified_at
    public String icon_reference; // wire: icon_reference
    public List<Object> allowed_entities; // wire: allowed_entities
    /** Build `AiSingleAgentResponse` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON) or an open extra bag, so those are detached, the
     * typed shell is deserialized, then they are reattached from the
     * raw tree (D-140). */
    public static AiSingleAgentResponse deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_allowed_entities = raw.remove('allowed_entities');
        AiSingleAgentResponse result = (AiSingleAgentResponse) JSON.deserialize(JSON.serialize(raw), AiSingleAgentResponse.class);
        result.allowed_entities = (List<Object>) d_allowed_entities;
        return result;
    }
}
