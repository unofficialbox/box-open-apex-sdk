// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `WorkflowFlows` schema (6 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields rename between the wire and Apex, so this class carries
 * `normalizeKeys`/`denormalizeKeys` (used by the managers) to remap
 * JSON keys around native (de)serialization. */
public class WorkflowFlows {
    public String id; // wire: id
    public String type; // wire: type
    public FlowsTrigger trigger_r; // wire: trigger
    public List<FlowsOutcomes> outcomes; // wire: outcomes
    public Datetime created_at; // wire: created_at
    public UserBase created_by; // wire: created_by
    /** Wire → Apex: JSON keys for the `WorkflowFlows` wire shape (see class doc). */
    public static Map<String, Object> normalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('trigger')) {
            Object v = raw.remove('trigger');
            if (v instanceof Map<String, Object>) v = FlowsTrigger.normalizeKeys((Map<String, Object>) v);
            raw.put('trigger_r', v);
        }
        return raw;
    }
    /** Apex → wire: JSON keys for the `WorkflowFlows` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('trigger_r')) {
            Object v = raw.remove('trigger_r');
            if (v instanceof Map<String, Object>) v = FlowsTrigger.denormalizeKeys((Map<String, Object>) v);
            raw.put('trigger', v);
        }
        return raw;
    }
}
