// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `archives` resource manager — 4 operation(s).
 * Reached through the `Box` client (`client.archives`), never
 * constructed directly.
 */
public with sharing class BoxArchives {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxArchives(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /archives`
     *
     * @param limit_r query parameter, optional
     * @param marker query parameter, optional
     * @return `Archives`
     */
    public Archives list_r(Long limit_r, String marker) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'archives';
        if (limit_r != null) request.query.put('limit', String.valueOf(limit_r));
        if (marker != null) request.query.put('marker', String.valueOf(marker));
        request.headers.put('box-version', '2025.0');
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = Archives.normalizeKeys((Map<String, Object>) parsed);
        return (Archives) JSON.deserialize(JSON.serialize(parsed), Archives.class);
    }

    /**
     * `POST /archives`
     *
     * @param body the request body (`CreateArchiveRequest`)
     * @return `Archive`
     */
    public Archive create(CreateArchiveRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'archives';
        request.headers.put('box-version', '2025.0');
        request.body = body;
        BoxResponse response = client.send(request);
        return (Archive) JSON.deserialize(response.body, Archive.class);
    }

    /**
     * `PUT /archives/{archive_id}`
     *
     * @param archiveId path parameter
     * @param body the request body (`UpdateArchiveRequest`)
     * @return `Archive`
     */
    public Archive update_r(String archiveId, UpdateArchiveRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'api';
        request.path = '/' + 'archives' + '/' + archiveId;
        request.headers.put('box-version', '2025.0');
        request.body = body;
        BoxResponse response = client.send(request);
        return (Archive) JSON.deserialize(response.body, Archive.class);
    }

    /**
     * `DELETE /archives/{archive_id}`
     *
     * @param archiveId path parameter
     */
    public void delete_r(String archiveId) {
        BoxRequest request = new BoxRequest();
        request.method = 'DELETE';
        request.baseUrl = 'api';
        request.path = '/' + 'archives' + '/' + archiveId;
        request.headers.put('box-version', '2025.0');
        BoxResponse response = client.send(request);
    }
}
