// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `email_aliases` resource manager — 3 operation(s).
 * Reached through the `Box` client (`client.emailAliases`), never
 * constructed directly.
 */
public with sharing class BoxEmailAliases {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxEmailAliases(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /users/{user_id}/email_aliases`
     *
     * @param userId path parameter
     * @return `EmailAliases`
     */
    public EmailAliases listUser(String userId) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'users' + '/' + userId + '/' + 'email_aliases';
        BoxResponse response = client.send(request);
        return (EmailAliases) JSON.deserialize(response.body, EmailAliases.class);
    }

    /**
     * `POST /users/{user_id}/email_aliases`
     *
     * @param userId path parameter
     * @param body the request body (`CreateUserIdRequest`)
     * @return `EmailAlias`
     */
    public EmailAlias createUser(String userId, CreateUserIdRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'users' + '/' + userId + '/' + 'email_aliases';
        request.body = body;
        BoxResponse response = client.send(request);
        return (EmailAlias) JSON.deserialize(response.body, EmailAlias.class);
    }

    /**
     * `DELETE /users/{user_id}/email_aliases/{email_alias_id}`
     *
     * @param userId path parameter
     * @param emailAliasId path parameter
     */
    public void deleteUser(String userId, String emailAliasId) {
        BoxRequest request = new BoxRequest();
        request.method = 'DELETE';
        request.baseUrl = 'api';
        request.path = '/' + 'users' + '/' + userId + '/' + 'email_aliases' + '/' + emailAliasId;
        BoxResponse response = client.send(request);
    }
}
