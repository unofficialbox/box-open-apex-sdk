// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `file_versions` resource manager — 5 operation(s).
 * Reached through the `Box` client (`client.fileVersions`), never
 * constructed directly.
 */
public with sharing class BoxFileVersions {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxFileVersions(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /files/{file_id}/versions`
     *
     * @param fileId path parameter
     * @param fields query parameter, optional
     * @param limit_r query parameter, optional
     * @param offset query parameter, optional
     * @return `FileVersions`
     */
    public FileVersions listFileVersions(String fileId, List<String> fields, Long limit_r, Long offset) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId + '/' + 'versions';
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        if (limit_r != null) request.query.put('limit', String.valueOf(limit_r));
        if (offset != null) request.query.put('offset', String.valueOf(offset));
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = FileVersions.normalizeKeys((Map<String, Object>) parsed);
        return (FileVersions) JSON.deserialize(JSON.serialize(parsed), FileVersions.class);
    }

    /**
     * `GET /files/{file_id}/versions/{file_version_id}`
     *
     * @param fileId path parameter
     * @param fileVersionId path parameter
     * @param fields query parameter, optional
     * @return `FileVersionFull`
     */
    public FileVersionFull getFileVersion(String fileId, String fileVersionId, List<String> fields) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId + '/' + 'versions' + '/' + fileVersionId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        BoxResponse response = client.send(request);
        return (FileVersionFull) JSON.deserialize(response.body, FileVersionFull.class);
    }

    /**
     * `PUT /files/{file_id}/versions/{file_version_id}`
     *
     * @param fileId path parameter
     * @param fileVersionId path parameter
     * @param body the request body (`UpdateFileVersionRequest`)
     * @return `FileVersionFull`
     */
    public FileVersionFull updateFileVersion(String fileId, String fileVersionId, UpdateFileVersionRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId + '/' + 'versions' + '/' + fileVersionId;
        Object wireBody = JSON.deserializeUntyped(JSON.serialize(body, true));
            if (wireBody instanceof Map<String, Object>) wireBody = UpdateFileVersionRequest.denormalizeKeys((Map<String, Object>) wireBody);
        request.body = wireBody;
        request.suppressNulls = false;
        BoxResponse response = client.send(request);
        return (FileVersionFull) JSON.deserialize(response.body, FileVersionFull.class);
    }

    /**
     * `DELETE /files/{file_id}/versions/{file_version_id}`
     *
     * @param fileId path parameter
     * @param fileVersionId path parameter
     * @param ifMatch header parameter, optional
     */
    public void deleteFileVersion(String fileId, String fileVersionId, String ifMatch) {
        BoxRequest request = new BoxRequest();
        request.method = 'DELETE';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId + '/' + 'versions' + '/' + fileVersionId;
        if (ifMatch != null) request.headers.put('if-match', String.valueOf(ifMatch));
        BoxResponse response = client.send(request);
    }

    /**
     * `POST /files/{file_id}/versions/current`
     *
     * @param fileId path parameter
     * @param fields query parameter, optional
     * @param body the request body (`CreateFileVersionCurrentRequest`)
     * @return `FileVersionFull`
     */
    public FileVersionFull createFileVersionCurrent(String fileId, List<String> fields, CreateFileVersionCurrentRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId + '/' + 'versions' + '/' + 'current';
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        request.body = body;
        BoxResponse response = client.send(request);
        return (FileVersionFull) JSON.deserialize(response.body, FileVersionFull.class);
    }
}
