// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `FileFullExpiringEmbedLink` schema (5 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields rename between the wire and Apex, so this class carries
 * `normalizeKeys`/`denormalizeKeys` (used by the managers) to remap
 * JSON keys around native (de)serialization.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class FileFullExpiringEmbedLink {
    public String access_token; // wire: access_token
    public Long expires_in; // wire: expires_in
    public String token_type; // wire: token_type
    public List<ResourceScope> restricted_to; // wire: restricted_to
    public String url; // wire: url
    /** Wire → Apex: JSON keys for the `FileFullExpiringEmbedLink` wire shape (see class doc). */
    public static Map<String, Object> normalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('restricted_to')) {
            Object v = raw.remove('restricted_to');
            if (v instanceof List<Object>) {
                List<Object> wLst0 = (List<Object>) v;
                for (Integer wIdx0 = 0; wIdx0 < wLst0.size(); wIdx0++) {
                    Object wElem0 = wLst0[wIdx0];
            if (wElem0 instanceof Map<String, Object>) wElem0 = ResourceScope.normalizeKeys((Map<String, Object>) wElem0);
                    wLst0[wIdx0] = wElem0;
                }
            }
            raw.put('restricted_to', v);
        }
        return raw;
    }
    /** Apex → wire: JSON keys for the `FileFullExpiringEmbedLink` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('restricted_to')) {
            Object v = raw.remove('restricted_to');
            if (v instanceof List<Object>) {
                List<Object> wLst0 = (List<Object>) v;
                for (Integer wIdx0 = 0; wIdx0 < wLst0.size(); wIdx0++) {
                    Object wElem0 = wLst0[wIdx0];
            if (wElem0 instanceof Map<String, Object>) wElem0 = ResourceScope.denormalizeKeys((Map<String, Object>) wElem0);
                    wLst0[wIdx0] = wElem0;
                }
            }
            raw.put('restricted_to', v);
        }
        return raw;
    }
    /** Build `FileFullExpiringEmbedLink` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON) or an open extra bag, so those are detached, the
     * typed shell is deserialized, then they are reattached from the
     * raw tree (D-140). */
    public static FileFullExpiringEmbedLink deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_restricted_to = raw.remove('restricted_to');
        FileFullExpiringEmbedLink result = (FileFullExpiringEmbedLink) JSON.deserialize(JSON.serialize(FileFullExpiringEmbedLink.normalizeKeys(raw)), FileFullExpiringEmbedLink.class);
        if (d_restricted_to == null) {
            result.restricted_to = null;
        } else {
            List<ResourceScope> dLst0 = new List<ResourceScope>();
            for (Object dEl0 : (List<Object>) d_restricted_to) {
                ResourceScope dEv0;
        dEv0 = ResourceScope.deserialize(dEl0);
                dLst0.add(dEv0);
            }
            result.restricted_to = dLst0;
        }
        return result;
    }
}
