// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `TemplateSignerInput` schema (16 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class TemplateSignerInput {
    public String document_tag_id; // wire: document_tag_id
    public String text_value; // wire: text_value
    public Boolean checkbox_value; // wire: checkbox_value
    public Date date_value; // wire: date_value
    public String type; // wire: type
    public String content_type; // wire: content_type
    public Boolean is_required; // wire: is_required
    public Long page_index; // wire: page_index
    public String document_id; // wire: document_id
    public List<String> dropdown_choices; // wire: dropdown_choices
    public String group_id; // wire: group_id
    public TemplateSignerInputCoordinates coordinates; // wire: coordinates
    public TemplateSignerInputDimensions dimensions; // wire: dimensions
    public String label; // wire: label
    public Boolean read_only; // wire: read_only
    public Object validation; // wire: validation
    /** Build `TemplateSignerInput` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON) or an open extra bag, so those are detached, the
     * typed shell is deserialized, then they are reattached from the
     * raw tree (D-140). */
    public static TemplateSignerInput deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_validation = raw.remove('validation');
        TemplateSignerInput result = (TemplateSignerInput) JSON.deserialize(JSON.serialize(raw), TemplateSignerInput.class);
        result.validation = d_validation;
        return result;
    }
}
