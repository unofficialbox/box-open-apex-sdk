// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `avatars` resource manager — 3 operation(s).
 * Reached through the `Box` client (`client.avatars`), never
 * constructed directly.
 */
public with sharing class BoxAvatars {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxAvatars(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /users/{user_id}/avatar`
     *
     * @param userId path parameter
     * @return `Blob`
     */
    public Blob getUserAvatar(String userId) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'users' + '/' + userId + '/' + 'avatar';
        BoxResponse response = client.send(request);
        return response.binaryBody;
    }

    /**
     * `POST /users/{user_id}/avatar`
     *
     * @param userId path parameter
     * @param body the request body (`CreateUserAvatarRequest`)
     * @return `UserAvatar`
     */
    public UserAvatar createUserAvatar(String userId, CreateUserAvatarRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'users' + '/' + userId + '/' + 'avatar';
        request.multipartFilePartName = 'pic';
        request.multipartFile = body.pic;
        BoxResponse response = client.send(request);
        return (UserAvatar) JSON.deserialize(response.body, UserAvatar.class);
    }

    /**
     * `DELETE /users/{user_id}/avatar`
     *
     * @param userId path parameter
     */
    public void deleteUserAvatar(String userId) {
        BoxRequest request = new BoxRequest();
        request.method = 'DELETE';
        request.baseUrl = 'api';
        request.path = '/' + 'users' + '/' + userId + '/' + 'avatar';
        BoxResponse response = client.send(request);
    }
}
