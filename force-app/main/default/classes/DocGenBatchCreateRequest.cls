// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `DocGenBatchCreateRequest` schema (6 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class DocGenBatchCreateRequest {
    public FileReference file; // wire: file
    public FileVersionBase file_version; // wire: file_version
    public String input_source; // wire: input_source
    public DocGenBatchCreateRequestDestinat_8adff70 destination_folder; // wire: destination_folder
    public String output_type; // wire: output_type
    public List<DocGenDocumentGenerationData> document_generation_data; // wire: document_generation_data
    /** Build `DocGenBatchCreateRequest` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON) or an open extra bag, so those are detached, the
     * typed shell is deserialized, then they are reattached from the
     * raw tree (D-140). */
    public static DocGenBatchCreateRequest deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_document_generation_data = raw.remove('document_generation_data');
        DocGenBatchCreateRequest result = (DocGenBatchCreateRequest) JSON.deserialize(JSON.serialize(raw), DocGenBatchCreateRequest.class);
        if (d_document_generation_data == null) {
            result.document_generation_data = null;
        } else {
            List<DocGenDocumentGenerationData> dLst0 = new List<DocGenDocumentGenerationData>();
            for (Object dEl0 : (List<Object>) d_document_generation_data) {
                DocGenDocumentGenerationData dEv0;
        dEv0 = DocGenDocumentGenerationData.deserialize(dEl0);
                dLst0.add(dEv0);
            }
            result.document_generation_data = dLst0;
        }
        return result;
    }
}
