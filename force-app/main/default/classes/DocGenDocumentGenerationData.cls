// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `DocGenDocumentGenerationData` schema (2 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class DocGenDocumentGenerationData {
    public String generated_file_name; // wire: generated_file_name
    public Map<String, Object> user_input; // wire: user_input
    /** Build `DocGenDocumentGenerationData` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON), so those are detached, the typed shell is
     * deserialized, then they are reattached from the raw tree (D-140). */
    public static DocGenDocumentGenerationData deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_user_input = raw.remove('user_input');
        DocGenDocumentGenerationData result = (DocGenDocumentGenerationData) JSON.deserialize(JSON.serialize(raw), DocGenDocumentGenerationData.class);
        result.user_input = (Map<String, Object>) d_user_input;
        return result;
    }
}
