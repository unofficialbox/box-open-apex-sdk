// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `UserUpdateRequest` schema (21 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields rename between the wire and Apex, so this class carries
 * `normalizeKeys`/`denormalizeKeys` (used by the managers) to remap
 * JSON keys around native (de)serialization.
 * Explicit null (D-138): to clear a field on update, add its Apex
 * field name to `fieldsToNull` — the request sends JSON `null` for it;
 * fields you leave unset stay absent. */
public class UserUpdateRequest {
    public String enterprise; // wire: enterprise
    public Boolean notify; // wire: notify
    public String name; // wire: name
    public String login; // wire: login
    public String role; // wire: role
    public String language; // wire: language
    public Boolean is_sync_enabled; // wire: is_sync_enabled
    public String job_title; // wire: job_title
    public String phone; // wire: phone
    public String address; // wire: address
    public List<TrackingCode> tracking_codes; // wire: tracking_codes
    public Boolean can_see_managed_users; // wire: can_see_managed_users
    public String timezone; // wire: timezone
    public Boolean is_external_collab_restricted; // wire: is_external_collab_restricted
    public Boolean is_exempt_from_device_limits; // wire: is_exempt_from_device_limits
    public Boolean is_exempt_from_login_verification; // wire: is_exempt_from_login_verification
    public Boolean is_password_reset_required; // wire: is_password_reset_required
    public String status; // wire: status
    public Long space_amount; // wire: space_amount
    public PutIdNotificationEmail notification_email; // wire: notification_email
    public String external_app_user_id; // wire: external_app_user_id
    // Explicit-null control (D-138): Apex field names to send as JSON `null`.
    public Set<String> fieldsToNull = new Set<String>();
    /** Wire → Apex: JSON keys for the `UserUpdateRequest` wire shape (see class doc). */
    public static Map<String, Object> normalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('tracking_codes')) {
            Object v = raw.remove('tracking_codes');
            if (v instanceof List<Object>) {
                List<Object> wLst0 = (List<Object>) v;
                for (Integer wIdx0 = 0; wIdx0 < wLst0.size(); wIdx0++) {
                    Object wElem0 = wLst0[wIdx0];
            if (wElem0 instanceof Map<String, Object>) wElem0 = TrackingCode.normalizeKeys((Map<String, Object>) wElem0);
                    wLst0[wIdx0] = wElem0;
                }
            }
            raw.put('tracking_codes', v);
        }
        return raw;
    }
    /** Apex → wire: JSON keys for the `UserUpdateRequest` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('tracking_codes')) {
            Object v = raw.remove('tracking_codes');
            if (v instanceof List<Object>) {
                List<Object> wLst0 = (List<Object>) v;
                for (Integer wIdx0 = 0; wIdx0 < wLst0.size(); wIdx0++) {
                    Object wElem0 = wLst0[wIdx0];
            if (wElem0 instanceof Map<String, Object>) wElem0 = TrackingCode.denormalizeKeys((Map<String, Object>) wElem0);
                    wLst0[wIdx0] = wElem0;
                }
            }
            raw.put('tracking_codes', v);
        }
        // Explicit null (D-138): send `null` for each field the caller listed.
        Object toNull = raw.remove('fieldsToNull');
        if (toNull instanceof List<Object>) {
            for (Object nf : (List<Object>) toNull) {
                String nfName = String.valueOf(nf);
                if (nfName == 'enterprise') raw.put('enterprise', null);
                if (nfName == 'notification_email') raw.put('notification_email', null);
            }
        }
        return raw;
    }
}
