// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `trashed_files` resource manager — 3 operation(s).
 * Reached through the `Box` client (`client.trashedFiles`), never
 * constructed directly.
 */
public with sharing class BoxTrashedFiles {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxTrashedFiles(BoxClient client) {
        this.client = client;
    }

    /**
     * `POST /files/{file_id}`
     *
     * @param fileId path parameter
     * @param fields query parameter, optional
     * @param body the request body (`CreateFileRequest`)
     * @return `TrashFileRestored`
     */
    public TrashFileRestored createFile(String fileId, List<String> fields, CreateFileRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        request.body = body;
        BoxResponse response = client.send(request);
        return (TrashFileRestored) JSON.deserialize(response.body, TrashFileRestored.class);
    }

    /**
     * `GET /files/{file_id}/trash`
     *
     * @param fileId path parameter
     * @param fields query parameter, optional
     * @return `TrashFile`
     */
    public TrashFile getFileTrash(String fileId, List<String> fields) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId + '/' + 'trash';
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        BoxResponse response = client.send(request);
        return (TrashFile) JSON.deserialize(response.body, TrashFile.class);
    }

    /**
     * `DELETE /files/{file_id}/trash`
     *
     * @param fileId path parameter
     */
    public void deleteFileTrash(String fileId) {
        BoxRequest request = new BoxRequest();
        request.method = 'DELETE';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId + '/' + 'trash';
        BoxResponse response = client.send(request);
    }
}
