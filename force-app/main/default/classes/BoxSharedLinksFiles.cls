// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `shared_links_files` resource manager — 5 operation(s).
 * Reached through the `Box` client (`client.sharedLinksFiles`), never
 * constructed directly.
 */
public with sharing class BoxSharedLinksFiles {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxSharedLinksFiles(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /shared_items`
     *
     * @param ifNoneMatch header parameter, optional
     * @param fields query parameter, optional
     * @param boxapi header parameter
     * @return `FileFull`
     */
    public FileFull getSharedItems(String ifNoneMatch, List<String> fields, String boxapi) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'shared_items';
        if (ifNoneMatch != null) request.headers.put('if-none-match', String.valueOf(ifNoneMatch));
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        if (boxapi != null) request.headers.put('boxapi', String.valueOf(boxapi));
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        FileFull deserialized;
        deserialized = FileFull.deserialize(parsed);
        return deserialized;
    }

    /**
     * `GET /files/{file_id}`
     *
     * @param fileId path parameter
     * @param fields query parameter
     * @return `FileFull`
     */
    public FileFull getFileGetSharedLink(String fileId, String fields) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        FileFull deserialized;
        deserialized = FileFull.deserialize(parsed);
        return deserialized;
    }

    /**
     * `PUT /files/{file_id}`
     *
     * @param fileId path parameter
     * @param fields query parameter
     * @param body the request body (`FileUpdateRequest2`)
     * @return `FileFull`
     */
    public FileFull updateFileAddSharedLink(String fileId, String fields, FileUpdateRequest2 body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        Object wireBody = JSON.deserializeUntyped(JSON.serialize(body, true));
            if (wireBody instanceof Map<String, Object>) wireBody = FileUpdateRequest2.denormalizeKeys((Map<String, Object>) wireBody);
        request.body = wireBody;
        request.suppressNulls = false;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        FileFull deserialized;
        deserialized = FileFull.deserialize(parsed);
        return deserialized;
    }

    /**
     * `PUT /files/{file_id}`
     *
     * @param fileId path parameter
     * @param fields query parameter
     * @param body the request body (`FileUpdateRequest2`)
     * @return `FileFull`
     */
    public FileFull updateFileUpdateSharedLink(String fileId, String fields, FileUpdateRequest2 body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        Object wireBody = JSON.deserializeUntyped(JSON.serialize(body, true));
            if (wireBody instanceof Map<String, Object>) wireBody = FileUpdateRequest2.denormalizeKeys((Map<String, Object>) wireBody);
        request.body = wireBody;
        request.suppressNulls = false;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        FileFull deserialized;
        deserialized = FileFull.deserialize(parsed);
        return deserialized;
    }

    /**
     * `PUT /files/{file_id}`
     *
     * @param fileId path parameter
     * @param fields query parameter
     * @param body the request body (`FileUpdateRequest3`)
     * @return `FileFull`
     */
    public FileFull updateFileRemoveSharedLink(String fileId, String fields, FileUpdateRequest3 body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        Object wireBody = JSON.deserializeUntyped(JSON.serialize(body, true));
            if (wireBody instanceof Map<String, Object>) wireBody = FileUpdateRequest3.denormalizeKeys((Map<String, Object>) wireBody);
        request.body = wireBody;
        request.suppressNulls = false;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        FileFull deserialized;
        deserialized = FileFull.deserialize(parsed);
        return deserialized;
    }
}
