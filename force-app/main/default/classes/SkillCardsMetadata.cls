// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `SkillCardsMetadata` schema (9 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields rename between the wire and Apex, so this class carries
 * `normalizeKeys`/`denormalizeKeys` (used by the managers) to remap
 * JSON keys around native (de)serialization.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class SkillCardsMetadata {
    public Boolean canEdit; // wire: $canEdit
    public String id; // wire: $id
    public String parent; // wire: $parent
    public String scope; // wire: $scope
    public String template; // wire: $template
    public String type; // wire: $type
    public Long typeVersion; // wire: $typeVersion
    public Long version; // wire: $version
    public List<Object> cards; // wire: cards
    /** Wire → Apex: JSON keys for the `SkillCardsMetadata` wire shape (see class doc). */
    public static Map<String, Object> normalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('$canEdit')) {
            Object v = raw.remove('$canEdit');
            raw.put('canEdit', v);
        }
        if (raw.containsKey('$id')) {
            Object v = raw.remove('$id');
            raw.put('id', v);
        }
        if (raw.containsKey('$parent')) {
            Object v = raw.remove('$parent');
            raw.put('parent', v);
        }
        if (raw.containsKey('$scope')) {
            Object v = raw.remove('$scope');
            raw.put('scope', v);
        }
        if (raw.containsKey('$template')) {
            Object v = raw.remove('$template');
            raw.put('template', v);
        }
        if (raw.containsKey('$type')) {
            Object v = raw.remove('$type');
            raw.put('type', v);
        }
        if (raw.containsKey('$typeVersion')) {
            Object v = raw.remove('$typeVersion');
            raw.put('typeVersion', v);
        }
        if (raw.containsKey('$version')) {
            Object v = raw.remove('$version');
            raw.put('version', v);
        }
        return raw;
    }
    /** Apex → wire: JSON keys for the `SkillCardsMetadata` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('canEdit')) {
            Object v = raw.remove('canEdit');
            raw.put('$canEdit', v);
        }
        if (raw.containsKey('id')) {
            Object v = raw.remove('id');
            raw.put('$id', v);
        }
        if (raw.containsKey('parent')) {
            Object v = raw.remove('parent');
            raw.put('$parent', v);
        }
        if (raw.containsKey('scope')) {
            Object v = raw.remove('scope');
            raw.put('$scope', v);
        }
        if (raw.containsKey('template')) {
            Object v = raw.remove('template');
            raw.put('$template', v);
        }
        if (raw.containsKey('type')) {
            Object v = raw.remove('type');
            raw.put('$type', v);
        }
        if (raw.containsKey('typeVersion')) {
            Object v = raw.remove('typeVersion');
            raw.put('$typeVersion', v);
        }
        if (raw.containsKey('version')) {
            Object v = raw.remove('version');
            raw.put('$version', v);
        }
        return raw;
    }
    /** Build `SkillCardsMetadata` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON) or an open extra bag, so those are detached, the
     * typed shell is deserialized, then they are reattached from the
     * raw tree (D-140). */
    public static SkillCardsMetadata deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_cards = raw.remove('cards');
        SkillCardsMetadata result = (SkillCardsMetadata) JSON.deserialize(JSON.serialize(SkillCardsMetadata.normalizeKeys(raw)), SkillCardsMetadata.class);
        result.cards = (List<Object>) d_cards;
        return result;
    }
}
