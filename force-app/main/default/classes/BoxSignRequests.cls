// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `sign_requests` resource manager — 5 operation(s).
 * Reached through the `Box` client (`client.signRequests`), never
 * constructed directly.
 */
public with sharing class BoxSignRequests {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxSignRequests(BoxClient client) {
        this.client = client;
    }

    /**
     * `POST /sign_requests/{sign_request_id}/cancel`
     *
     * @param signRequestId path parameter
     * @param body the request body (`SignRequestCancelRequest`)
     * @return `SignRequest`
     */
    public SignRequest cancel(String signRequestId, SignRequestCancelRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'sign_requests' + '/' + signRequestId + '/' + 'cancel';
        request.body = body;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        SignRequest deserialized;
        deserialized = SignRequest.deserialize(parsed);
        return deserialized;
    }

    /**
     * `POST /sign_requests/{sign_request_id}/resend`
     *
     * @param signRequestId path parameter
     */
    public void resend(String signRequestId) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'sign_requests' + '/' + signRequestId + '/' + 'resend';
        BoxResponse response = client.send(request);
    }

    /**
     * `GET /sign_requests/{sign_request_id}`
     *
     * @param signRequestId path parameter
     * @return `SignRequest`
     */
    public SignRequest get(String signRequestId) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'sign_requests' + '/' + signRequestId;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        SignRequest deserialized;
        deserialized = SignRequest.deserialize(parsed);
        return deserialized;
    }

    /**
     * `GET /sign_requests`
     *
     * @param marker query parameter, optional
     * @param limit_r query parameter, optional
     * @param senders query parameter, optional
     * @param sharedRequests query parameter, optional
     * @return `SignRequests`
     */
    public SignRequests list_r(String marker, Long limit_r, List<String> senders, Boolean sharedRequests) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'sign_requests';
        if (marker != null) request.query.put('marker', String.valueOf(marker));
        if (limit_r != null) request.query.put('limit', String.valueOf(limit_r));
        if (senders != null) request.query.put('senders', String.valueOf(senders));
        if (sharedRequests != null) request.query.put('shared_requests', String.valueOf(sharedRequests));
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        SignRequests deserialized;
        deserialized = SignRequests.deserialize(parsed);
        return deserialized;
    }

    /**
     * `POST /sign_requests`
     *
     * @param body the request body (`SignRequestCreateRequest`)
     * @return `SignRequest`
     */
    public SignRequest create(SignRequestCreateRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'sign_requests';
        Object wireBody = JSON.deserializeUntyped(JSON.serialize(body, true));
            if (wireBody instanceof Map<String, Object>) wireBody = SignRequestCreateRequest.denormalizeKeys((Map<String, Object>) wireBody);
        request.body = wireBody;
        request.suppressNulls = false;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        SignRequest deserialized;
        deserialized = SignRequest.deserialize(parsed);
        return deserialized;
    }
}
