// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `storage_policies` resource manager — 2 operation(s).
 * Reached through the `Box` client (`client.storagePolicies`), never
 * constructed directly.
 */
public with sharing class BoxStoragePolicies {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxStoragePolicies(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /storage_policies`
     *
     * @param fields query parameter, optional
     * @param marker query parameter, optional
     * @param limit_r query parameter, optional
     * @return `StoragePolicies`
     */
    public StoragePolicies list_r(List<String> fields, String marker, Long limit_r) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'storage_policies';
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        if (marker != null) request.query.put('marker', String.valueOf(marker));
        if (limit_r != null) request.query.put('limit', String.valueOf(limit_r));
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = StoragePolicies.normalizeKeys((Map<String, Object>) parsed);
        return (StoragePolicies) JSON.deserialize(JSON.serialize(parsed), StoragePolicies.class);
    }

    /**
     * `GET /storage_policies/{storage_policy_id}`
     *
     * @param storagePolicyId path parameter
     * @return `StoragePolicy`
     */
    public StoragePolicy get(String storagePolicyId) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'storage_policies' + '/' + storagePolicyId;
        BoxResponse response = client.send(request);
        return (StoragePolicy) JSON.deserialize(response.body, StoragePolicy.class);
    }
}
