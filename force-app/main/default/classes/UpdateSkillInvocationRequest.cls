// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `UpdateSkillInvocationRequest` schema (5 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields rename between the wire and Apex, so this class carries
 * `normalizeKeys`/`denormalizeKeys` (used by the managers) to remap
 * JSON keys around native (de)serialization.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class UpdateSkillInvocationRequest {
    public String status; // wire: status
    public PutSkillInvocationIdMetadata metadata; // wire: metadata
    public PutSkillInvocationIdFile file; // wire: file
    public PutSkillInvocationIdFileVersion file_version; // wire: file_version
    public PutSkillInvocationIdUsage usage; // wire: usage
    /** Wire → Apex: JSON keys for the `UpdateSkillInvocationRequest` wire shape (see class doc). */
    public static Map<String, Object> normalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('usage')) {
            Object v = raw.remove('usage');
            if (v instanceof Map<String, Object>) v = PutSkillInvocationIdUsage.normalizeKeys((Map<String, Object>) v);
            raw.put('usage', v);
        }
        return raw;
    }
    /** Apex → wire: JSON keys for the `UpdateSkillInvocationRequest` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('usage')) {
            Object v = raw.remove('usage');
            if (v instanceof Map<String, Object>) v = PutSkillInvocationIdUsage.denormalizeKeys((Map<String, Object>) v);
            raw.put('usage', v);
        }
        return raw;
    }
    /** Build `UpdateSkillInvocationRequest` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON) or an open extra bag, so those are detached, the
     * typed shell is deserialized, then they are reattached from the
     * raw tree (D-140). */
    public static UpdateSkillInvocationRequest deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_metadata = raw.remove('metadata');
        UpdateSkillInvocationRequest result = (UpdateSkillInvocationRequest) JSON.deserialize(JSON.serialize(UpdateSkillInvocationRequest.normalizeKeys(raw)), UpdateSkillInvocationRequest.class);
        result.metadata = PutSkillInvocationIdMetadata.deserialize(d_metadata);
        return result;
    }
}
