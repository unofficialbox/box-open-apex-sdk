// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `SchemaUpdateRequest` schema (8 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class SchemaUpdateRequest {
    public String op; // wire: op
    public Map<String, Object> data; // wire: data
    public String fieldKey; // wire: fieldKey
    public List<String> fieldKeys; // wire: fieldKeys
    public String enumOptionKey; // wire: enumOptionKey
    public List<String> enumOptionKeys; // wire: enumOptionKeys
    public String multiSelectOptionKey; // wire: multiSelectOptionKey
    public List<String> multiSelectOptionKeys; // wire: multiSelectOptionKeys
    /** Build `SchemaUpdateRequest` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON), so those are detached, the typed shell is
     * deserialized, then they are reattached from the raw tree (D-140). */
    public static SchemaUpdateRequest deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_data = raw.remove('data');
        SchemaUpdateRequest result = (SchemaUpdateRequest) JSON.deserialize(JSON.serialize(raw), SchemaUpdateRequest.class);
        result.data = (Map<String, Object>) d_data;
        return result;
    }
}
