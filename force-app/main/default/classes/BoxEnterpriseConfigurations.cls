// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `enterprise_configurations` resource manager — 1 operation(s).
 * Reached through the `Box` client (`client.enterpriseConfigurations`), never
 * constructed directly.
 */
public with sharing class BoxEnterpriseConfigurations {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxEnterpriseConfigurations(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /enterprise_configurations/{enterprise_id}`
     *
     * @param enterpriseId path parameter
     * @param categories query parameter
     * @return `EnterpriseConfiguration`
     */
    public EnterpriseConfiguration get(String enterpriseId, List<String> categories) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'enterprise_configurations' + '/' + enterpriseId;
        if (categories != null) request.query.put('categories', String.valueOf(categories));
        request.headers.put('box-version', '2025.0');
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        EnterpriseConfiguration deserialized;
        deserialized = EnterpriseConfiguration.deserialize(parsed);
        return deserialized;
    }
}
