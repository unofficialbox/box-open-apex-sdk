// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `folder_locks` resource manager — 3 operation(s).
 * Reached through the `Box` client (`client.folderLocks`), never
 * constructed directly.
 */
public with sharing class BoxFolderLocks {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxFolderLocks(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /folder_locks`
     *
     * @param folderId query parameter
     * @return `FolderLocks`
     */
    public FolderLocks list_r(String folderId) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'folder_locks';
        if (folderId != null) request.query.put('folder_id', String.valueOf(folderId));
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = FolderLocks.normalizeKeys((Map<String, Object>) parsed);
        return (FolderLocks) JSON.deserialize(JSON.serialize(parsed), FolderLocks.class);
    }

    /**
     * `POST /folder_locks`
     *
     * @param body the request body (`FolderLockCreateRequest`)
     * @return `FolderLock`
     */
    public FolderLock create(FolderLockCreateRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'folder_locks';
        Object wireBody = JSON.deserializeUntyped(JSON.serialize(body, true));
            if (wireBody instanceof Map<String, Object>) wireBody = FolderLockCreateRequest.denormalizeKeys((Map<String, Object>) wireBody);
        request.body = wireBody;
        request.suppressNulls = false;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = FolderLock.normalizeKeys((Map<String, Object>) parsed);
        return (FolderLock) JSON.deserialize(JSON.serialize(parsed), FolderLock.class);
    }

    /**
     * `DELETE /folder_locks/{folder_lock_id}`
     *
     * @param folderLockId path parameter
     */
    public void delete_r(String folderLockId) {
        BoxRequest request = new BoxRequest();
        request.method = 'DELETE';
        request.baseUrl = 'api';
        request.path = '/' + 'folder_locks' + '/' + folderLockId;
        BoxResponse response = client.send(request);
    }
}
