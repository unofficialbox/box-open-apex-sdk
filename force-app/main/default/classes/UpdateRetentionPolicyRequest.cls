// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `UpdateRetentionPolicyRequest` schema (10 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * This class carries a `denormalizeKeys` write hook (used by the managers)
 * to shape request bodies for the wire.
 * Explicit null (D-138): to clear a field on update, add its Apex
 * field name to `fieldsToNull` — the request sends JSON `null` for it;
 * fields you leave unset stay absent.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class UpdateRetentionPolicyRequest {
    public String policy_name; // wire: policy_name
    public String description; // wire: description
    public Object disposition_action; // wire: disposition_action
    public String retention_type; // wire: retention_type
    public Object retention_length; // wire: retention_length
    public String status; // wire: status
    public Boolean can_owner_extend_retention; // wire: can_owner_extend_retention
    public Object max_extension_length; // wire: max_extension_length
    public Boolean are_owners_notified; // wire: are_owners_notified
    public List<UserBase> custom_notification_recipients; // wire: custom_notification_recipients
    // Explicit-null control (D-138): Apex field names to send as JSON `null`.
    public Set<String> fieldsToNull = new Set<String>();
    /** Apex → wire: JSON keys for the `UpdateRetentionPolicyRequest` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        // Explicit null (D-138): send `null` for each field the caller listed.
        Object toNull = raw.remove('fieldsToNull');
        if (toNull instanceof List<Object>) {
            for (Object nf : (List<Object>) toNull) {
                String nfName = String.valueOf(nf);
                if (nfName == 'policy_name') raw.put('policy_name', null);
                if (nfName == 'description') raw.put('description', null);
                if (nfName == 'retention_type') raw.put('retention_type', null);
                if (nfName == 'status') raw.put('status', null);
                if (nfName == 'can_owner_extend_retention') raw.put('can_owner_extend_retention', null);
                if (nfName == 'are_owners_notified') raw.put('are_owners_notified', null);
                if (nfName == 'custom_notification_recipients') raw.put('custom_notification_recipients', null);
            }
        }
        return raw;
    }
    /** Build `UpdateRetentionPolicyRequest` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON) or an open extra bag, so those are detached, the
     * typed shell is deserialized, then they are reattached from the
     * raw tree (D-140). */
    public static UpdateRetentionPolicyRequest deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_disposition_action = raw.remove('disposition_action');
        Object d_retention_length = raw.remove('retention_length');
        Object d_max_extension_length = raw.remove('max_extension_length');
        UpdateRetentionPolicyRequest result = (UpdateRetentionPolicyRequest) JSON.deserialize(JSON.serialize(raw), UpdateRetentionPolicyRequest.class);
        result.disposition_action = d_disposition_action;
        result.retention_length = d_retention_length;
        result.max_extension_length = d_max_extension_length;
        return result;
    }
}
