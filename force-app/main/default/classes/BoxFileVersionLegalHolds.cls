// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `file_version_legal_holds` resource manager — 2 operation(s).
 * Reached through the `Box` client (`client.fileVersionLegalHolds`), never
 * constructed directly.
 */
public with sharing class BoxFileVersionLegalHolds {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxFileVersionLegalHolds(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /file_version_legal_holds/{file_version_legal_hold_id}`
     *
     * @param fileVersionLegalHoldId path parameter
     * @return `FileVersionLegalHold`
     */
    public FileVersionLegalHold get(String fileVersionLegalHoldId) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'file_version_legal_holds' + '/' + fileVersionLegalHoldId;
        BoxResponse response = client.send(request);
        return (FileVersionLegalHold) JSON.deserialize(response.body, FileVersionLegalHold.class);
    }

    /**
     * `GET /file_version_legal_holds`
     *
     * @param policyId query parameter
     * @param marker query parameter, optional
     * @param limit_r query parameter, optional
     * @return `FileVersionLegalHolds`
     */
    public FileVersionLegalHolds list_r(String policyId, String marker, Long limit_r) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'file_version_legal_holds';
        if (policyId != null) request.query.put('policy_id', String.valueOf(policyId));
        if (marker != null) request.query.put('marker', String.valueOf(marker));
        if (limit_r != null) request.query.put('limit', String.valueOf(limit_r));
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = FileVersionLegalHolds.normalizeKeys((Map<String, Object>) parsed);
        return (FileVersionLegalHolds) JSON.deserialize(JSON.serialize(parsed), FileVersionLegalHolds.class);
    }
}
