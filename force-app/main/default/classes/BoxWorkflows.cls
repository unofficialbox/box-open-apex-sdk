// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `workflows` resource manager — 2 operation(s).
 * Reached through the `Box` client (`client.workflows`), never
 * constructed directly.
 */
public with sharing class BoxWorkflows {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxWorkflows(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /workflows`
     *
     * @param folderId query parameter
     * @param triggerType query parameter, optional
     * @param limit_r query parameter, optional
     * @param marker query parameter, optional
     * @return `Workflows`
     */
    public Workflows list_r(String folderId, String triggerType, Long limit_r, String marker) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'workflows';
        if (folderId != null) request.query.put('folder_id', String.valueOf(folderId));
        if (triggerType != null) request.query.put('trigger_type', String.valueOf(triggerType));
        if (limit_r != null) request.query.put('limit', String.valueOf(limit_r));
        if (marker != null) request.query.put('marker', String.valueOf(marker));
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = Workflows.normalizeKeys((Map<String, Object>) parsed);
        return (Workflows) JSON.deserialize(JSON.serialize(parsed), Workflows.class);
    }

    /**
     * `POST /workflows/{workflow_id}/start`
     *
     * @param workflowId path parameter
     * @param body the request body (`WorkflowStartRequest`)
     */
    public void start(String workflowId, WorkflowStartRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'workflows' + '/' + workflowId + '/' + 'start';
        request.body = body;
        BoxResponse response = client.send(request);
    }
}
