// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `events` resource manager — 2 operation(s).
 * Reached through the `Box` client (`client.events`), never
 * constructed directly.
 */
public with sharing class BoxEvents {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxEvents(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /events`
     *
     * @param streamType query parameter, optional
     * @param streamPosition query parameter, optional
     * @param limit_r query parameter, optional
     * @param eventType query parameter, optional
     * @param createdAfter query parameter, optional
     * @param createdBefore query parameter, optional
     * @return `Events`
     */
    public Events list_r(String streamType, String streamPosition, Long limit_r, List<String> eventType, Datetime createdAfter, Datetime createdBefore) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'events';
        if (streamType != null) request.query.put('stream_type', String.valueOf(streamType));
        if (streamPosition != null) request.query.put('stream_position', String.valueOf(streamPosition));
        if (limit_r != null) request.query.put('limit', String.valueOf(limit_r));
        if (eventType != null) request.query.put('event_type', String.valueOf(eventType));
        if (createdAfter != null) request.query.put('created_after', String.valueOf(createdAfter));
        if (createdBefore != null) request.query.put('created_before', String.valueOf(createdBefore));
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        Events deserialized;
        deserialized = Events.deserialize(parsed);
        return deserialized;
    }

    /**
     * `OPTIONS /events`
     * @return `RealtimeServers`
     */
    public RealtimeServers options() {
        BoxRequest request = new BoxRequest();
        request.method = 'OPTIONS';
        request.baseUrl = 'api';
        request.path = '/' + 'events';
        BoxResponse response = client.send(request);
        return (RealtimeServers) JSON.deserialize(response.body, RealtimeServers.class);
    }
}
