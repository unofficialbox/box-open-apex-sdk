// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `AiAgentExtractStructured` schema (4 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class AiAgentExtractStructured {
    public String type; // wire: type
    public AiAgentLongTextTool long_text; // wire: long_text
    public AiAgentBasicTextTool basic_text; // wire: basic_text
    public AiAgentBasicTextTool basic_image; // wire: basic_image
    /** Build `AiAgentExtractStructured` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON) or an open extra bag, so those are detached, the
     * typed shell is deserialized, then they are reattached from the
     * raw tree (D-140). */
    public static AiAgentExtractStructured deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_long_text = raw.remove('long_text');
        Object d_basic_text = raw.remove('basic_text');
        Object d_basic_image = raw.remove('basic_image');
        AiAgentExtractStructured result = (AiAgentExtractStructured) JSON.deserialize(JSON.serialize(raw), AiAgentExtractStructured.class);
        result.long_text = AiAgentLongTextTool.deserialize(d_long_text);
        result.basic_text = AiAgentBasicTextTool.deserialize(d_basic_text);
        result.basic_image = AiAgentBasicTextTool.deserialize(d_basic_image);
        return result;
    }
}
