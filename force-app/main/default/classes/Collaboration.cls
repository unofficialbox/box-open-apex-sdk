// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `Collaboration` schema (15 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class Collaboration {
    public String id; // wire: id
    public String type; // wire: type
    public Object item; // wire: item
    public AppItem app_item; // wire: app_item
    public Object accessible_by; // wire: accessible_by
    public String invite_email; // wire: invite_email
    public String role; // wire: role
    public Datetime expires_at; // wire: expires_at
    public Boolean is_access_only; // wire: is_access_only
    public String status; // wire: status
    public Datetime acknowledged_at; // wire: acknowledged_at
    public UserCollaborations created_by; // wire: created_by
    public Datetime created_at; // wire: created_at
    public Datetime modified_at; // wire: modified_at
    public CollaborationAcceptanceRequireme_fc18177 acceptance_requirements_status; // wire: acceptance_requirements_status
    /** Build `Collaboration` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON) or an open extra bag, so those are detached, the
     * typed shell is deserialized, then they are reattached from the
     * raw tree (D-140). */
    public static Collaboration deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_item = raw.remove('item');
        Object d_accessible_by = raw.remove('accessible_by');
        Collaboration result = (Collaboration) JSON.deserialize(JSON.serialize(raw), Collaboration.class);
        result.item = d_item;
        result.accessible_by = d_accessible_by;
        return result;
    }
}
