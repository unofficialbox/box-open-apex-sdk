// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `chunked_uploads` resource manager — 7 operation(s).
 * Reached through the `Box` client (`client.chunkedUploads`), never
 * constructed directly.
 */
public with sharing class BoxChunkedUploads {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxChunkedUploads(BoxClient client) {
        this.client = client;
    }

    /**
     * `POST /files/upload_sessions`
     *
     * @param body the request body (`FileUploadSessionCreateRequest`)
     * @return `UploadSession`
     */
    public UploadSession createFileUploadSession(FileUploadSessionCreateRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'upload';
        request.path = '/' + 'files' + '/' + 'upload_sessions';
        request.body = body;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = UploadSession.normalizeKeys((Map<String, Object>) parsed);
        return (UploadSession) JSON.deserialize(JSON.serialize(parsed), UploadSession.class);
    }

    /**
     * `POST /files/{file_id}/upload_sessions`
     *
     * @param fileId path parameter
     * @param body the request body (`FileVersionUploadSessionCreateRequest`)
     * @return `UploadSession`
     */
    public UploadSession createFileVersionUploadSession(String fileId, FileVersionUploadSessionCreateRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'upload';
        request.path = '/' + 'files' + '/' + fileId + '/' + 'upload_sessions';
        request.body = body;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = UploadSession.normalizeKeys((Map<String, Object>) parsed);
        return (UploadSession) JSON.deserialize(JSON.serialize(parsed), UploadSession.class);
    }

    /**
     * `GET /files/upload_sessions/{upload_session_id}`
     *
     * @param uploadSessionId path parameter
     * @return `UploadSession`
     */
    public UploadSession getFileUploadSession(String uploadSessionId) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'upload_session';
        request.path = '/' + 'files' + '/' + 'upload_sessions' + '/' + uploadSessionId;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = UploadSession.normalizeKeys((Map<String, Object>) parsed);
        return (UploadSession) JSON.deserialize(JSON.serialize(parsed), UploadSession.class);
    }

    /**
     * `PUT /files/upload_sessions/{upload_session_id}`
     *
     * @param uploadSessionId path parameter
     * @param digest header parameter
     * @param contentRange header parameter
     * @param body the request body (`Blob`)
     * @return `UploadedPart`
     */
    public UploadedPart updateFileUploadSession(String uploadSessionId, String digest, String contentRange, Blob body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'upload_session';
        request.path = '/' + 'files' + '/' + 'upload_sessions' + '/' + uploadSessionId;
        if (digest != null) request.headers.put('digest', String.valueOf(digest));
        if (contentRange != null) request.headers.put('content-range', String.valueOf(contentRange));
        request.binaryBody = body;
        BoxResponse response = client.send(request);
        return (UploadedPart) JSON.deserialize(response.body, UploadedPart.class);
    }

    /**
     * `DELETE /files/upload_sessions/{upload_session_id}`
     *
     * @param uploadSessionId path parameter
     */
    public void deleteFileUploadSession(String uploadSessionId) {
        BoxRequest request = new BoxRequest();
        request.method = 'DELETE';
        request.baseUrl = 'upload_session';
        request.path = '/' + 'files' + '/' + 'upload_sessions' + '/' + uploadSessionId;
        BoxResponse response = client.send(request);
    }

    /**
     * `GET /files/upload_sessions/{upload_session_id}/parts`
     *
     * @param uploadSessionId path parameter
     * @param offset query parameter, optional
     * @param limit_r query parameter, optional
     * @return `UploadParts`
     */
    public UploadParts listFileUploadSessionParts(String uploadSessionId, Long offset, Long limit_r) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'upload_session';
        request.path = '/' + 'files' + '/' + 'upload_sessions' + '/' + uploadSessionId + '/' + 'parts';
        if (offset != null) request.query.put('offset', String.valueOf(offset));
        if (limit_r != null) request.query.put('limit', String.valueOf(limit_r));
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = UploadParts.normalizeKeys((Map<String, Object>) parsed);
        return (UploadParts) JSON.deserialize(JSON.serialize(parsed), UploadParts.class);
    }

    /**
     * `POST /files/upload_sessions/{upload_session_id}/commit`
     *
     * @param uploadSessionId path parameter
     * @param digest header parameter
     * @param ifMatch header parameter, optional
     * @param ifNoneMatch header parameter, optional
     * @param body the request body (`FileUploadSessionCommitRequest`)
     * @return `Files`
     */
    public Files commitFileUploadSession(String uploadSessionId, String digest, String ifMatch, String ifNoneMatch, FileUploadSessionCommitRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'upload_session';
        request.path = '/' + 'files' + '/' + 'upload_sessions' + '/' + uploadSessionId + '/' + 'commit';
        if (digest != null) request.headers.put('digest', String.valueOf(digest));
        if (ifMatch != null) request.headers.put('if-match', String.valueOf(ifMatch));
        if (ifNoneMatch != null) request.headers.put('if-none-match', String.valueOf(ifNoneMatch));
        request.body = body;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        Files deserialized;
        deserialized = Files.deserialize(parsed);
        return deserialized;
    }
}
