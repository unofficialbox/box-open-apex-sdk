// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `CollectionsOrder` schema (2 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields rename between the wire and Apex, so this class carries
 * `normalizeKeys`/`denormalizeKeys` (used by the managers) to remap
 * JSON keys around native (de)serialization. */
public class CollectionsOrder {
    public String by_r; // wire: by
    public String direction; // wire: direction
    /** Wire → Apex: JSON keys for the `CollectionsOrder` wire shape (see class doc). */
    public static Map<String, Object> normalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('by')) {
            Object v = raw.remove('by');
            raw.put('by_r', v);
        }
        return raw;
    }
    /** Apex → wire: JSON keys for the `CollectionsOrder` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('by_r')) {
            Object v = raw.remove('by_r');
            raw.put('by', v);
        }
        return raw;
    }
}
