// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `RetentionPolicyAssignment` schema (8 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields rename between the wire and Apex, so this class carries
 * `normalizeKeys`/`denormalizeKeys` (used by the managers) to remap
 * JSON keys around native (de)serialization.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class RetentionPolicyAssignment {
    public String id; // wire: id
    public String type; // wire: type
    public RetentionPolicyMini retention_policy; // wire: retention_policy
    public RetentionPolicyAssignmentAssignedTo assigned_to; // wire: assigned_to
    public List<RetentionPolicyAssignmentFilterFields> filter_fields; // wire: filter_fields
    public UserMini assigned_by; // wire: assigned_by
    public Datetime assigned_at; // wire: assigned_at
    public String start_date_field; // wire: start_date_field
    /** Wire → Apex: JSON keys for the `RetentionPolicyAssignment` wire shape (see class doc). */
    public static Map<String, Object> normalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('filter_fields')) {
            Object v = raw.remove('filter_fields');
            if (v instanceof List<Object>) {
                List<Object> wLst0 = (List<Object>) v;
                for (Integer wIdx0 = 0; wIdx0 < wLst0.size(); wIdx0++) {
                    Object wElem0 = wLst0[wIdx0];
            if (wElem0 instanceof Map<String, Object>) wElem0 = RetentionPolicyAssignmentFilterFields.normalizeKeys((Map<String, Object>) wElem0);
                    wLst0[wIdx0] = wElem0;
                }
            }
            raw.put('filter_fields', v);
        }
        return raw;
    }
    /** Apex → wire: JSON keys for the `RetentionPolicyAssignment` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('filter_fields')) {
            Object v = raw.remove('filter_fields');
            if (v instanceof List<Object>) {
                List<Object> wLst0 = (List<Object>) v;
                for (Integer wIdx0 = 0; wIdx0 < wLst0.size(); wIdx0++) {
                    Object wElem0 = wLst0[wIdx0];
            if (wElem0 instanceof Map<String, Object>) wElem0 = RetentionPolicyAssignmentFilterFields.denormalizeKeys((Map<String, Object>) wElem0);
                    wLst0[wIdx0] = wElem0;
                }
            }
            raw.put('filter_fields', v);
        }
        return raw;
    }
    /** Build `RetentionPolicyAssignment` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON), so those are detached, the typed shell is
     * deserialized, then they are reattached from the raw tree (D-140). */
    public static RetentionPolicyAssignment deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_retention_policy = raw.remove('retention_policy');
        RetentionPolicyAssignment result = (RetentionPolicyAssignment) JSON.deserialize(JSON.serialize(RetentionPolicyAssignment.normalizeKeys(raw)), RetentionPolicyAssignment.class);
        result.retention_policy = RetentionPolicyMini.deserialize(d_retention_policy);
        return result;
    }
}
