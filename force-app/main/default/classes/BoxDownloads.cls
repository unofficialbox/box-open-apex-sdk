// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `downloads` resource manager — 1 operation(s).
 * Reached through the `Box` client (`client.downloads`), never
 * constructed directly.
 */
public with sharing class BoxDownloads {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxDownloads(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /files/{file_id}/content`
     *
     * @param fileId path parameter
     * @param range header parameter, optional
     * @param boxapi header parameter, optional
     * @param version query parameter, optional
     * @param accessToken query parameter, optional
     * @return `Blob`
     */
    public Blob getFileContent(String fileId, String range, String boxapi, String version, String accessToken) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId + '/' + 'content';
        if (range != null) request.headers.put('range', String.valueOf(range));
        if (boxapi != null) request.headers.put('boxapi', String.valueOf(boxapi));
        if (version != null) request.query.put('version', String.valueOf(version));
        if (accessToken != null) request.query.put('access_token', String.valueOf(accessToken));
        BoxResponse response = client.send(request);
        return response.binaryBody;
    }
}
