// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `sign_templates` resource manager — 2 operation(s).
 * Reached through the `Box` client (`client.signTemplates`), never
 * constructed directly.
 */
public with sharing class BoxSignTemplates {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxSignTemplates(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /sign_templates`
     *
     * @param marker query parameter, optional
     * @param limit_r query parameter, optional
     * @return `SignTemplates`
     */
    public SignTemplates list_r(String marker, Long limit_r) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'sign_templates';
        if (marker != null) request.query.put('marker', String.valueOf(marker));
        if (limit_r != null) request.query.put('limit', String.valueOf(limit_r));
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        SignTemplates deserialized;
        deserialized = SignTemplates.deserialize(parsed);
        return deserialized;
    }

    /**
     * `GET /sign_templates/{template_id}`
     *
     * @param templateId path parameter
     * @return `SignTemplate`
     */
    public SignTemplate get(String templateId) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'sign_templates' + '/' + templateId;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        SignTemplate deserialized;
        deserialized = SignTemplate.deserialize(parsed);
        return deserialized;
    }
}
