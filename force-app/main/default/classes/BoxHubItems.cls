// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `hub_items` resource manager — 2 operation(s).
 * Reached through the `Box` client (`client.hubItems`), never
 * constructed directly.
 */
public with sharing class BoxHubItems {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxHubItems(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /hub_items`
     *
     * @param hubId query parameter
     * @param parentId query parameter, optional
     * @param marker query parameter, optional
     * @param limit_r query parameter, optional
     * @return `HubItems`
     */
    public HubItems list_r(String hubId, String parentId, String marker, Long limit_r) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'hub_items';
        if (hubId != null) request.query.put('hub_id', String.valueOf(hubId));
        if (parentId != null) request.query.put('parent_id', String.valueOf(parentId));
        if (marker != null) request.query.put('marker', String.valueOf(marker));
        if (limit_r != null) request.query.put('limit', String.valueOf(limit_r));
        request.headers.put('box-version', '2025.0');
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
            if (parsed instanceof Map<String, Object>) parsed = HubItems.normalizeKeys((Map<String, Object>) parsed);
        return (HubItems) JSON.deserialize(JSON.serialize(parsed), HubItems.class);
    }

    /**
     * `POST /hubs/{hub_id}/manage_items`
     *
     * @param hubId path parameter
     * @param body the request body (`HubItemsManageRequest`)
     * @return `HubItemsManageResponse`
     */
    public HubItemsManageResponse createHubManageItems(String hubId, HubItemsManageRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'hubs' + '/' + hubId + '/' + 'manage_items';
        request.headers.put('box-version', '2025.0');
        request.body = body;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        HubItemsManageResponse deserialized;
        deserialized = HubItemsManageResponse.deserialize(parsed);
        return deserialized;
    }
}
