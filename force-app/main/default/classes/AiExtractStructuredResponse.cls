// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `AiExtractStructuredResponse` schema (6 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class AiExtractStructuredResponse {
    public AiExtractResponse answer; // wire: answer
    public Datetime created_at; // wire: created_at
    public String completion_reason; // wire: completion_reason
    public Map<String, Object> confidence_score; // wire: confidence_score
    public Map<String, Object> reference; // wire: reference
    public AiAgentInfo ai_agent_info; // wire: ai_agent_info
    /** Build `AiExtractStructuredResponse` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON), so those are detached, the typed shell is
     * deserialized, then they are reattached from the raw tree (D-140). */
    public static AiExtractStructuredResponse deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_confidence_score = raw.remove('confidence_score');
        Object d_reference = raw.remove('reference');
        AiExtractStructuredResponse result = (AiExtractStructuredResponse) JSON.deserialize(JSON.serialize(raw), AiExtractStructuredResponse.class);
        result.confidence_score = (Map<String, Object>) d_confidence_score;
        result.reference = (Map<String, Object>) d_reference;
        return result;
    }
}
