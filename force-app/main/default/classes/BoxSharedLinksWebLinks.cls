// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `shared_links_web_links` resource manager — 5 operation(s).
 * Reached through the `Box` client (`client.sharedLinksWebLinks`), never
 * constructed directly.
 */
public with sharing class BoxSharedLinksWebLinks {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxSharedLinksWebLinks(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /shared_items`
     *
     * @param ifNoneMatch header parameter, optional
     * @param fields query parameter, optional
     * @param boxapi header parameter
     * @return `WebLink`
     */
    public WebLink getSharedItemsWebLinks(String ifNoneMatch, List<String> fields, String boxapi) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'shared_items';
        if (ifNoneMatch != null) request.headers.put('if-none-match', String.valueOf(ifNoneMatch));
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        if (boxapi != null) request.headers.put('boxapi', String.valueOf(boxapi));
        BoxResponse response = client.send(request);
        return (WebLink) JSON.deserialize(response.body, WebLink.class);
    }

    /**
     * `GET /web_links/{web_link_id}`
     *
     * @param webLinkId path parameter
     * @param fields query parameter
     * @return `WebLink`
     */
    public WebLink getWebLinkGetSharedLink(String webLinkId, String fields) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'web_links' + '/' + webLinkId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        BoxResponse response = client.send(request);
        return (WebLink) JSON.deserialize(response.body, WebLink.class);
    }

    /**
     * `PUT /web_links/{web_link_id}`
     *
     * @param webLinkId path parameter
     * @param fields query parameter
     * @param body the request body (`UpdateFileAddSharedLinkRequest`)
     * @return `WebLink`
     */
    public WebLink updateWebLinkAddSharedLink(String webLinkId, String fields, UpdateFileAddSharedLinkRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'api';
        request.path = '/' + 'web_links' + '/' + webLinkId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        Object wireBody = JSON.deserializeUntyped(JSON.serialize(body, true));
            if (wireBody instanceof Map<String, Object>) wireBody = UpdateFileAddSharedLinkRequest.denormalizeKeys((Map<String, Object>) wireBody);
        request.body = wireBody;
        request.suppressNulls = false;
        BoxResponse response = client.send(request);
        return (WebLink) JSON.deserialize(response.body, WebLink.class);
    }

    /**
     * `PUT /web_links/{web_link_id}`
     *
     * @param webLinkId path parameter
     * @param fields query parameter
     * @param body the request body (`UpdateFileAddSharedLinkRequest`)
     * @return `WebLink`
     */
    public WebLink updateWebLinkUpdateSharedLink(String webLinkId, String fields, UpdateFileAddSharedLinkRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'api';
        request.path = '/' + 'web_links' + '/' + webLinkId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        Object wireBody = JSON.deserializeUntyped(JSON.serialize(body, true));
            if (wireBody instanceof Map<String, Object>) wireBody = UpdateFileAddSharedLinkRequest.denormalizeKeys((Map<String, Object>) wireBody);
        request.body = wireBody;
        request.suppressNulls = false;
        BoxResponse response = client.send(request);
        return (WebLink) JSON.deserialize(response.body, WebLink.class);
    }

    /**
     * `PUT /web_links/{web_link_id}`
     *
     * @param webLinkId path parameter
     * @param fields query parameter
     * @param body the request body (`UpdateFileRemoveSharedLinkRequest`)
     * @return `WebLink`
     */
    public WebLink updateWebLinkRemoveSharedLink(String webLinkId, String fields, UpdateFileRemoveSharedLinkRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'api';
        request.path = '/' + 'web_links' + '/' + webLinkId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        Object wireBody = JSON.deserializeUntyped(JSON.serialize(body, true));
            if (wireBody instanceof Map<String, Object>) wireBody = UpdateFileRemoveSharedLinkRequest.denormalizeKeys((Map<String, Object>) wireBody);
        request.body = wireBody;
        request.suppressNulls = false;
        BoxResponse response = client.send(request);
        return (WebLink) JSON.deserialize(response.body, WebLink.class);
    }
}
