// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `tasks` resource manager — 5 operation(s).
 * Reached through the `Box` client (`client.tasks`), never
 * constructed directly.
 */
public with sharing class BoxTasks {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxTasks(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /files/{file_id}/tasks`
     *
     * @param fileId path parameter
     * @return `Tasks`
     */
    public Tasks listFile(String fileId) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + fileId + '/' + 'tasks';
        BoxResponse response = client.send(request);
        return (Tasks) JSON.deserialize(response.body, Tasks.class);
    }

    /**
     * `POST /tasks`
     *
     * @param body the request body (`CreateTaskRequest`)
     * @return `Task`
     */
    public Task create(CreateTaskRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'tasks';
        request.body = body;
        BoxResponse response = client.send(request);
        return (Task) JSON.deserialize(response.body, Task.class);
    }

    /**
     * `GET /tasks/{task_id}`
     *
     * @param taskId path parameter
     * @return `Task`
     */
    public Task get(String taskId) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'tasks' + '/' + taskId;
        BoxResponse response = client.send(request);
        return (Task) JSON.deserialize(response.body, Task.class);
    }

    /**
     * `PUT /tasks/{task_id}`
     *
     * @param taskId path parameter
     * @param body the request body (`UpdateTaskRequest`)
     * @return `Task`
     */
    public Task update_r(String taskId, UpdateTaskRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'api';
        request.path = '/' + 'tasks' + '/' + taskId;
        request.body = body;
        BoxResponse response = client.send(request);
        return (Task) JSON.deserialize(response.body, Task.class);
    }

    /**
     * `DELETE /tasks/{task_id}`
     *
     * @param taskId path parameter
     */
    public void delete_r(String taskId) {
        BoxRequest request = new BoxRequest();
        request.method = 'DELETE';
        request.baseUrl = 'api';
        request.path = '/' + 'tasks' + '/' + taskId;
        BoxResponse response = client.send(request);
    }
}
