// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `FileUpdateRequest` schema (9 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * This class carries a `denormalizeKeys` write hook (used by the managers)
 * to shape request bodies for the wire.
 * Explicit null (D-138): to clear a field on update, add its Apex
 * field name to `fieldsToNull` — the request sends JSON `null` for it;
 * fields you leave unset stay absent. */
public class FileUpdateRequest {
    public String name; // wire: name
    public String description; // wire: description
    public PutIdParent parent; // wire: parent
    public PutIdSharedLink shared_link; // wire: shared_link
    public PutIdLock lock; // wire: lock
    public Datetime disposition_at; // wire: disposition_at
    public PutIdPermissions permissions; // wire: permissions
    public List<CommentItem> collections; // wire: collections
    public List<String> tags; // wire: tags
    // Explicit-null control (D-138): Apex field names to send as JSON `null`.
    public Set<String> fieldsToNull = new Set<String>();
    /** Apex → wire: JSON keys for the `FileUpdateRequest` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('shared_link')) {
            Object v = raw.remove('shared_link');
            if (v instanceof Map<String, Object>) v = PutIdSharedLink.denormalizeKeys((Map<String, Object>) v);
            raw.put('shared_link', v);
        }
        // Explicit null (D-138): send `null` for each field the caller listed.
        Object toNull = raw.remove('fieldsToNull');
        if (toNull instanceof List<Object>) {
            for (Object nf : (List<Object>) toNull) {
                String nfName = String.valueOf(nf);
                if (nfName == 'shared_link') raw.put('shared_link', null);
                if (nfName == 'lock') raw.put('lock', null);
                if (nfName == 'collections') raw.put('collections', null);
            }
        }
        return raw;
    }
}
