// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `UploadSessionSessionEndpoints` schema (6 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields rename between the wire and Apex, so this class carries
 * `normalizeKeys`/`denormalizeKeys` (used by the managers) to remap
 * JSON keys around native (de)serialization. */
public class UploadSessionSessionEndpoints {
    public String upload_part; // wire: upload_part
    public String commit_r; // wire: commit
    public String abort; // wire: abort
    public String list_parts; // wire: list_parts
    public String status; // wire: status
    public String log_event; // wire: log_event
    /** Wire → Apex: JSON keys for the `UploadSessionSessionEndpoints` wire shape (see class doc). */
    public static Map<String, Object> normalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('commit')) {
            Object v = raw.remove('commit');
            raw.put('commit_r', v);
        }
        return raw;
    }
    /** Apex → wire: JSON keys for the `UploadSessionSessionEndpoints` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('commit_r')) {
            Object v = raw.remove('commit_r');
            raw.put('commit', v);
        }
        return raw;
    }
}
