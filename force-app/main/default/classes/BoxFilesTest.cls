// Code generated by box-gantry 0.1.0. DO NOT EDIT.
@isTest
private class BoxFilesTest {
    @isTest
    static void exercisesEveryOperation() {
        BoxCalloutMock mock = new BoxCalloutMock();
        BoxFiles svc = new BoxFiles(mock);
        Test.startTest();
        mock.body = '{}';
        svc.get('x', new List<String>(), 'x', 'x', 'x');
        mock.body = '{}';
        svc.update_r('x', new List<String>(), 'x', new FileUpdateRequest());
        mock.body = '{}';
        svc.delete_r('x', 'x');
        mock.body = '{}';
        svc.copy('x', new List<String>(), new FileCopyRequest());
        mock.bodyBlob = Blob.valueOf('x');
        svc.getThumbnail('x', 'x', 1, 1, 1, 1);
        Test.stopTest();
        System.assertNotEquals(null, mock.lastRequest);
    }
}
