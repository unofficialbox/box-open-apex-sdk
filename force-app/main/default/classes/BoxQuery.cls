// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `query` resource manager — 2 operation(s).
 * Reached through the `Box` client (`client.query`), never
 * constructed directly.
 */
public with sharing class BoxQuery {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxQuery(BoxClient client) {
        this.client = client;
    }

    /**
     * `POST /query`
     *
     * @param body the request body (`QueryRequestBody`)
     * @return `QueryResults`
     */
    public QueryResults query(QueryRequestBody body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'query';
        request.headers.put('box-version', '2026.0');
        Object wireBody = JSON.deserializeUntyped(JSON.serialize(body, true));
            if (wireBody instanceof Map<String, Object>) wireBody = QueryRequestBody.denormalizeKeys((Map<String, Object>) wireBody);
        request.body = wireBody;
        request.suppressNulls = false;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        QueryResults deserialized;
        deserialized = QueryResults.deserialize(parsed);
        return deserialized;
    }

    /**
     * `POST /query_insights`
     *
     * @param body the request body (`QueryInsightsRequestBody`)
     * @return `QueryInsights`
     */
    public QueryInsights queryInsights(QueryInsightsRequestBody body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'api';
        request.path = '/' + 'query_insights';
        request.headers.put('box-version', '2026.0');
        request.body = body;
        BoxResponse response = client.send(request);
        return (QueryInsights) JSON.deserialize(response.body, QueryInsights.class);
    }
}
