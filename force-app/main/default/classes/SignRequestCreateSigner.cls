// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `SignRequestCreateSigner` schema (13 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * This class carries a `denormalizeKeys` write hook (used by the managers)
 * to shape request bodies for the wire.
 * Explicit null (D-138): to clear a field on update, add its Apex
 * field name to `fieldsToNull` — the request sends JSON `null` for it;
 * fields you leave unset stay absent. */
public class SignRequestCreateSigner {
    public String email; // wire: email
    public String role; // wire: role
    public Boolean is_in_person; // wire: is_in_person
    public Long order; // wire: order
    public String embed_url_external_user_id; // wire: embed_url_external_user_id
    public String redirect_url; // wire: redirect_url
    public String declined_redirect_url; // wire: declined_redirect_url
    public Boolean login_required; // wire: login_required
    public String verification_phone_number; // wire: verification_phone_number
    public String password; // wire: password
    public String signer_group_id; // wire: signer_group_id
    public Boolean suppress_notifications; // wire: suppress_notifications
    public String language; // wire: language
    // Explicit-null control (D-138): Apex field names to send as JSON `null`.
    public Set<String> fieldsToNull = new Set<String>();
    /** Apex → wire: JSON keys for the `SignRequestCreateSigner` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        // Explicit null (D-138): send `null` for each field the caller listed.
        Object toNull = raw.remove('fieldsToNull');
        if (toNull instanceof List<Object>) {
            for (Object nf : (List<Object>) toNull) {
                String nfName = String.valueOf(nf);
                if (nfName == 'email') raw.put('email', null);
                if (nfName == 'embed_url_external_user_id') raw.put('embed_url_external_user_id', null);
                if (nfName == 'redirect_url') raw.put('redirect_url', null);
                if (nfName == 'declined_redirect_url') raw.put('declined_redirect_url', null);
                if (nfName == 'login_required') raw.put('login_required', null);
                if (nfName == 'verification_phone_number') raw.put('verification_phone_number', null);
                if (nfName == 'password') raw.put('password', null);
                if (nfName == 'signer_group_id') raw.put('signer_group_id', null);
                if (nfName == 'suppress_notifications') raw.put('suppress_notifications', null);
                if (nfName == 'language') raw.put('language', null);
            }
        }
        return raw;
    }
}
