// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `FilesOnHold` schema (4 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields rename between the wire and Apex, so this class carries
 * `normalizeKeys`/`denormalizeKeys` (used by the managers) to remap
 * JSON keys around native (de)serialization. */
public class FilesOnHold {
    public Long limit_r; // wire: limit
    public String next_marker; // wire: next_marker
    public String prev_marker; // wire: prev_marker
    public List<FileMini> entries; // wire: entries
    /** Wire → Apex: JSON keys for the `FilesOnHold` wire shape (see class doc). */
    public static Map<String, Object> normalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('limit')) {
            Object v = raw.remove('limit');
            raw.put('limit_r', v);
        }
        return raw;
    }
    /** Apex → wire: JSON keys for the `FilesOnHold` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('limit_r')) {
            Object v = raw.remove('limit_r');
            raw.put('limit', v);
        }
        return raw;
    }
}
