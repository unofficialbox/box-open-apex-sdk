// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `transfer` resource manager — 1 operation(s).
 * Reached through the `Box` client (`client.transfer`), never
 * constructed directly.
 */
public with sharing class BoxTransfer {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxTransfer(BoxClient client) {
        this.client = client;
    }

    /**
     * `PUT /users/{user_id}/folders/0`
     *
     * @param userId path parameter
     * @param fields query parameter, optional
     * @param notify query parameter, optional
     * @param body the request body (`TransferFoldersRequest`)
     * @return `FolderFull`
     */
    public FolderFull transferFolders(String userId, List<String> fields, Boolean notify, TransferFoldersRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'api';
        request.path = '/' + 'users' + '/' + userId + '/' + 'folders' + '/' + '0';
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        if (notify != null) request.query.put('notify', String.valueOf(notify));
        request.body = body;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        FolderFull deserialized;
        deserialized = FolderFull.deserialize(parsed);
        return deserialized;
    }
}
