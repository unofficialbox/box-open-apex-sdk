// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `QueryResultEntry` schema (2 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), or this class
 * carries an open extra bag (additionalProperties) — either way native
 * `JSON.deserialize` can't populate them, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140,
 * D-196). */
public class QueryResultEntry {
    public String id; // wire: id
    public String type; // wire: type
    public Map<String, Object> extra; // additionalProperties
    /** Build `QueryResultEntry` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON) or an open extra bag, so those are detached, the
     * typed shell is deserialized, then they are reattached from the
     * raw tree (D-140). */
    public static QueryResultEntry deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        // Extra bag (D-196): every key no named field claims. Computed
        // and stripped before the shell deserialize below — an unrecognized
        // JSON property makes native `JSON.deserialize` throw.
        Map<String, Object> extraRaw = raw.clone();
        extraRaw.remove('id');
        extraRaw.remove('type');
        for (String extraKey : extraRaw.keySet()) {
            raw.remove(extraKey);
        }
        QueryResultEntry result = (QueryResultEntry) JSON.deserialize(JSON.serialize(raw), QueryResultEntry.class);
        result.extra = (Map<String, Object>) extraRaw;
        return result;
    }
}
