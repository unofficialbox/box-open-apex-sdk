// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `Event` schema (9 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class Event {
    public String type; // wire: type
    public Datetime created_at; // wire: created_at
    public Datetime recorded_at; // wire: recorded_at
    public String event_id; // wire: event_id
    public UserMini created_by; // wire: created_by
    public String event_type; // wire: event_type
    public String session_id; // wire: session_id
    public Object source; // wire: source
    public Map<String, Object> additional_details; // wire: additional_details
    /** Build `Event` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON), so those are detached, the typed shell is
     * deserialized, then they are reattached from the raw tree (D-140). */
    public static Event deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_source = raw.remove('source');
        Object d_additional_details = raw.remove('additional_details');
        Event result = (Event) JSON.deserialize(JSON.serialize(raw), Event.class);
        result.source = d_source;
        result.additional_details = (Map<String, Object>) d_additional_details;
        return result;
    }
}
