// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `SignTemplate` schema (18 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * This class carries a `denormalizeKeys` write hook (used by the managers)
 * to shape request bodies for the wire.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class SignTemplate {
    public String type; // wire: type
    public String id; // wire: id
    public String name; // wire: name
    public String email_subject; // wire: email_subject
    public String email_message; // wire: email_message
    public Long days_valid; // wire: days_valid
    public FolderMini parent_folder; // wire: parent_folder
    public List<FileMini> source_files; // wire: source_files
    public Boolean are_fields_locked; // wire: are_fields_locked
    public Boolean are_options_locked; // wire: are_options_locked
    public Boolean are_recipients_locked; // wire: are_recipients_locked
    public Boolean are_email_settings_locked; // wire: are_email_settings_locked
    public Boolean are_files_locked; // wire: are_files_locked
    public List<TemplateSigner> signers; // wire: signers
    public SignTemplateAdditionalInfo additional_info; // wire: additional_info
    public SignTemplateReadySignLink ready_sign_link; // wire: ready_sign_link
    public SignTemplateCustomBranding custom_branding; // wire: custom_branding
    public String request_flow; // wire: request_flow
    /** Apex → wire: JSON keys for the `SignTemplate` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('parent_folder')) {
            Object v = raw.remove('parent_folder');
            if (v instanceof Map<String, Object>) v = FolderMini.denormalizeKeys((Map<String, Object>) v);
            raw.put('parent_folder', v);
        }
        return raw;
    }
    /** Build `SignTemplate` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON), so those are detached, the typed shell is
     * deserialized, then they are reattached from the raw tree (D-140). */
    public static SignTemplate deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_signers = raw.remove('signers');
        SignTemplate result = (SignTemplate) JSON.deserialize(JSON.serialize(raw), SignTemplate.class);
        if (d_signers == null) {
            result.signers = null;
        } else {
            List<TemplateSigner> dLst0 = new List<TemplateSigner>();
            for (Object dEl0 : (List<Object>) d_signers) {
                TemplateSigner dEv0;
        dEv0 = TemplateSigner.deserialize(dEl0);
                dLst0.add(dEv0);
            }
            result.signers = dLst0;
        }
        return result;
    }
}
