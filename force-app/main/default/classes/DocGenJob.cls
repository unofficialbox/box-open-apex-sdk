// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `DocGenJob` schema (10 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class DocGenJob {
    public String id; // wire: id
    public String type; // wire: type
    public DocGenBatchBase batch; // wire: batch
    public FileReference template_file; // wire: template_file
    public FileVersionBase template_file_version; // wire: template_file_version
    public Object output_file; // wire: output_file
    public Object output_file_version; // wire: output_file_version
    public String status; // wire: status
    public String output_type; // wire: output_type
    public DocGenJobFailures failures; // wire: failures
    /** Build `DocGenJob` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON), so those are detached, the typed shell is
     * deserialized, then they are reattached from the raw tree (D-140). */
    public static DocGenJob deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_output_file = raw.remove('output_file');
        Object d_output_file_version = raw.remove('output_file_version');
        DocGenJob result = (DocGenJob) JSON.deserialize(JSON.serialize(raw), DocGenJob.class);
        result.output_file = d_output_file;
        result.output_file_version = d_output_file_version;
        return result;
    }
}
