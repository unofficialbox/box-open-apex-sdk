// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `CreateAiAgent` schema (8 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * This class carries a `denormalizeKeys` write hook (used by the managers)
 * to shape request bodies for the wire.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class CreateAiAgent {
    public String type; // wire: type
    public String name; // wire: name
    public String access_state; // wire: access_state
    public String icon_reference; // wire: icon_reference
    public List<Object> allowed_entities; // wire: allowed_entities
    public AiStudioAgentAsk ask; // wire: ask
    public AiStudioAgentTextGen text_gen; // wire: text_gen
    public AiStudioAgentExtract extract; // wire: extract
    /** Apex → wire: JSON keys for the `CreateAiAgent` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('ask')) {
            Object v = raw.remove('ask');
            if (v instanceof Map<String, Object>) v = AiStudioAgentAsk.denormalizeKeys((Map<String, Object>) v);
            raw.put('ask', v);
        }
        if (raw.containsKey('text_gen')) {
            Object v = raw.remove('text_gen');
            if (v instanceof Map<String, Object>) v = AiStudioAgentTextGen.denormalizeKeys((Map<String, Object>) v);
            raw.put('text_gen', v);
        }
        if (raw.containsKey('extract')) {
            Object v = raw.remove('extract');
            if (v instanceof Map<String, Object>) v = AiStudioAgentExtract.denormalizeKeys((Map<String, Object>) v);
            raw.put('extract', v);
        }
        return raw;
    }
    /** Build `CreateAiAgent` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON), so those are detached, the typed shell is
     * deserialized, then they are reattached from the raw tree (D-140). */
    public static CreateAiAgent deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_allowed_entities = raw.remove('allowed_entities');
        Object d_ask = raw.remove('ask');
        Object d_text_gen = raw.remove('text_gen');
        Object d_extract = raw.remove('extract');
        CreateAiAgent result = (CreateAiAgent) JSON.deserialize(JSON.serialize(raw), CreateAiAgent.class);
        result.allowed_entities = (List<Object>) d_allowed_entities;
        result.ask = AiStudioAgentAsk.deserialize(d_ask);
        result.text_gen = AiStudioAgentTextGen.deserialize(d_text_gen);
        result.extract = AiStudioAgentExtract.deserialize(d_extract);
        return result;
    }
}
