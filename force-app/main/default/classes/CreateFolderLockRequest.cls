// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `CreateFolderLockRequest` schema (2 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields rename between the wire and Apex, so this class carries
 * `normalizeKeys`/`denormalizeKeys` (used by the managers) to remap
 * JSON keys around native (de)serialization. */
public class CreateFolderLockRequest {
    public FolderLockLockedOperations locked_operations; // wire: locked_operations
    public PostFolder folder; // wire: folder
    /** Wire → Apex: JSON keys for the `CreateFolderLockRequest` wire shape (see class doc). */
    public static Map<String, Object> normalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('locked_operations')) {
            Object v = raw.remove('locked_operations');
            if (v instanceof Map<String, Object>) v = FolderLockLockedOperations.normalizeKeys((Map<String, Object>) v);
            raw.put('locked_operations', v);
        }
        return raw;
    }
    /** Apex → wire: JSON keys for the `CreateFolderLockRequest` wire shape (see class doc). */
    public static Map<String, Object> denormalizeKeys(Map<String, Object> raw) {
        if (raw == null) return null;
        if (raw.containsKey('locked_operations')) {
            Object v = raw.remove('locked_operations');
            if (v instanceof Map<String, Object>) v = FolderLockLockedOperations.denormalizeKeys((Map<String, Object>) v);
            raw.put('locked_operations', v);
        }
        return raw;
    }
}
