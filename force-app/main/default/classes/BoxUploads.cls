// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `uploads` resource manager — 3 operation(s).
 * Reached through the `Box` client (`client.uploads`), never
 * constructed directly.
 */
public with sharing class BoxUploads {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxUploads(BoxClient client) {
        this.client = client;
    }

    /**
     * `POST /files/{file_id}/content`
     *
     * @param fileId path parameter
     * @param ifMatch header parameter, optional
     * @param fields query parameter, optional
     * @param contentMd5 header parameter, optional
     * @param body the request body (`FileIdContentCreateRequest`)
     * @return `Files`
     */
    public Files createFileContent(String fileId, String ifMatch, List<String> fields, String contentMd5, FileIdContentCreateRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'upload';
        request.path = '/' + 'files' + '/' + fileId + '/' + 'content';
        if (ifMatch != null) request.headers.put('if-match', String.valueOf(ifMatch));
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        if (contentMd5 != null) request.headers.put('content-md5', String.valueOf(contentMd5));
        request.body = body;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        Files deserialized;
        deserialized = Files.deserialize(parsed);
        return deserialized;
    }

    /**
     * `POST /files/content`
     *
     * @param fields query parameter, optional
     * @param contentMd5 header parameter, optional
     * @param body the request body (`FileContentCreateRequest`)
     * @return `Files`
     */
    public Files createFileContent2(List<String> fields, String contentMd5, FileContentCreateRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'POST';
        request.baseUrl = 'upload';
        request.path = '/' + 'files' + '/' + 'content';
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        if (contentMd5 != null) request.headers.put('content-md5', String.valueOf(contentMd5));
        request.body = body;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        Files deserialized;
        deserialized = Files.deserialize(parsed);
        return deserialized;
    }

    /**
     * `OPTIONS /files/content`
     *
     * @param body the request body (`OptionFileContentRequest`)
     * @return `UploadUrl`
     */
    public UploadUrl optionFileContent(OptionFileContentRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'OPTIONS';
        request.baseUrl = 'api';
        request.path = '/' + 'files' + '/' + 'content';
        request.body = body;
        BoxResponse response = client.send(request);
        return (UploadUrl) JSON.deserialize(response.body, UploadUrl.class);
    }
}
