// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `HubItemOperationResult` schema (5 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class HubItemOperationResult {
    public String action; // wire: action
    public Object item; // wire: item
    public String parent_id; // wire: parent_id
    public Long status; // wire: status
    public String error; // wire: error
    /** Build `HubItemOperationResult` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON), so those are detached, the typed shell is
     * deserialized, then they are reattached from the raw tree (D-140). */
    public static HubItemOperationResult deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_item = raw.remove('item');
        HubItemOperationResult result = (HubItemOperationResult) JSON.deserialize(JSON.serialize(raw), HubItemOperationResult.class);
        result.item = d_item;
        return result;
    }
}
