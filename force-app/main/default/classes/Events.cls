// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `Events` schema (3 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class Events {
    public Long chunk_size; // wire: chunk_size
    public Object next_stream_position; // wire: next_stream_position
    public List<Event> entries; // wire: entries
    /** Build `Events` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON) or an open extra bag, so those are detached, the
     * typed shell is deserialized, then they are reattached from the
     * raw tree (D-140). */
    public static Events deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_next_stream_position = raw.remove('next_stream_position');
        Object d_entries = raw.remove('entries');
        Events result = (Events) JSON.deserialize(JSON.serialize(raw), Events.class);
        result.next_stream_position = d_next_stream_position;
        if (d_entries == null) {
            result.entries = null;
        } else {
            List<Event> dLst0 = new List<Event>();
            for (Object dEl0 : (List<Object>) d_entries) {
                Event dEv0;
        dEv0 = Event.deserialize(dEl0);
                dLst0.add(dEv0);
            }
            result.entries = dLst0;
        }
        return result;
    }
}
