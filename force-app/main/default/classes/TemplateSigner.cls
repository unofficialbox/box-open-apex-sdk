// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/** Model for the `TemplateSigner` schema (11 field(s)). Fields
 * carry their wire (JSON) name in a trailing comment.
 * Some fields are `Object` (a union or free-form JSON), which native
 * `JSON.deserialize` can't populate, so this class carries a static
 * `deserialize(Object)` the managers call to read a response (D-140). */
public class TemplateSigner {
    public List<TemplateSignerInput> inputs; // wire: inputs
    public String email; // wire: email
    public String role; // wire: role
    public Boolean is_in_person; // wire: is_in_person
    public Long order; // wire: order
    public String signer_group_id; // wire: signer_group_id
    public String label; // wire: label
    public String public_id; // wire: public_id
    public Boolean is_password_required; // wire: is_password_required
    public Boolean is_phone_number_required; // wire: is_phone_number_required
    public Boolean login_required; // wire: login_required
    /** Build `TemplateSigner` from the untyped JSON tree. Apex's typed
     * `JSON.deserialize` can't populate an `Object` field (a union or
     * free-form JSON) or an open extra bag, so those are detached, the
     * typed shell is deserialized, then they are reattached from the
     * raw tree (D-140). */
    public static TemplateSigner deserialize(Object rawInput) {
        if (rawInput == null) return null;
        Map<String, Object> raw = ((Map<String, Object>) rawInput).clone();
        Object d_inputs = raw.remove('inputs');
        TemplateSigner result = (TemplateSigner) JSON.deserialize(JSON.serialize(raw), TemplateSigner.class);
        if (d_inputs == null) {
            result.inputs = null;
        } else {
            List<TemplateSignerInput> dLst0 = new List<TemplateSignerInput>();
            for (Object dEl0 : (List<Object>) d_inputs) {
                TemplateSignerInput dEv0;
        dEv0 = TemplateSignerInput.deserialize(dEl0);
                dLst0.add(dEv0);
            }
            result.inputs = dLst0;
        }
        return result;
    }
}
