// Code generated by box-gantry 0.1.0. DO NOT EDIT.
/**
 * The `shared_links_folders` resource manager — 5 operation(s).
 * Reached through the `Box` client (`client.sharedLinksFolders`), never
 * constructed directly.
 */
public with sharing class BoxSharedLinksFolders {
    private final BoxClient client;
    /** Wire the manager to the shared runtime client. */
    public BoxSharedLinksFolders(BoxClient client) {
        this.client = client;
    }

    /**
     * `GET /shared_items`
     *
     * @param ifNoneMatch header parameter, optional
     * @param fields query parameter, optional
     * @param boxapi header parameter
     * @return `FolderFull`
     */
    public FolderFull getSharedItemsFolders(String ifNoneMatch, List<String> fields, String boxapi) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'shared_items';
        if (ifNoneMatch != null) request.headers.put('if-none-match', String.valueOf(ifNoneMatch));
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        if (boxapi != null) request.headers.put('boxapi', String.valueOf(boxapi));
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        FolderFull deserialized;
        deserialized = FolderFull.deserialize(parsed);
        return deserialized;
    }

    /**
     * `GET /folders/{folder_id}`
     *
     * @param folderId path parameter
     * @param fields query parameter
     * @return `FolderFull`
     */
    public FolderFull getFolderGetSharedLink(String folderId, String fields) {
        BoxRequest request = new BoxRequest();
        request.method = 'GET';
        request.baseUrl = 'api';
        request.path = '/' + 'folders' + '/' + folderId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        FolderFull deserialized;
        deserialized = FolderFull.deserialize(parsed);
        return deserialized;
    }

    /**
     * `PUT /folders/{folder_id}`
     *
     * @param folderId path parameter
     * @param fields query parameter
     * @param body the request body (`UpdateFileAddSharedLinkRequest`)
     * @return `FolderFull`
     */
    public FolderFull updateFolderAddSharedLink(String folderId, String fields, UpdateFileAddSharedLinkRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'api';
        request.path = '/' + 'folders' + '/' + folderId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        Object wireBody = JSON.deserializeUntyped(JSON.serialize(body, true));
            if (wireBody instanceof Map<String, Object>) wireBody = UpdateFileAddSharedLinkRequest.denormalizeKeys((Map<String, Object>) wireBody);
        request.body = wireBody;
        request.suppressNulls = false;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        FolderFull deserialized;
        deserialized = FolderFull.deserialize(parsed);
        return deserialized;
    }

    /**
     * `PUT /folders/{folder_id}`
     *
     * @param folderId path parameter
     * @param fields query parameter
     * @param body the request body (`UpdateFolderUpdateSharedLinkRequest`)
     * @return `FolderFull`
     */
    public FolderFull updateFolderUpdateSharedLink(String folderId, String fields, UpdateFolderUpdateSharedLinkRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'api';
        request.path = '/' + 'folders' + '/' + folderId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        request.body = body;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        FolderFull deserialized;
        deserialized = FolderFull.deserialize(parsed);
        return deserialized;
    }

    /**
     * `PUT /folders/{folder_id}`
     *
     * @param folderId path parameter
     * @param fields query parameter
     * @param body the request body (`UpdateFileRemoveSharedLinkRequest`)
     * @return `FolderFull`
     */
    public FolderFull updateFolderRemoveSharedLink(String folderId, String fields, UpdateFileRemoveSharedLinkRequest body) {
        BoxRequest request = new BoxRequest();
        request.method = 'PUT';
        request.baseUrl = 'api';
        request.path = '/' + 'folders' + '/' + folderId;
        if (fields != null) request.query.put('fields', String.valueOf(fields));
        Object wireBody = JSON.deserializeUntyped(JSON.serialize(body, true));
            if (wireBody instanceof Map<String, Object>) wireBody = UpdateFileRemoveSharedLinkRequest.denormalizeKeys((Map<String, Object>) wireBody);
        request.body = wireBody;
        request.suppressNulls = false;
        BoxResponse response = client.send(request);
        Object parsed = JSON.deserializeUntyped(response.body);
        FolderFull deserialized;
        deserialized = FolderFull.deserialize(parsed);
        return deserialized;
    }
}
